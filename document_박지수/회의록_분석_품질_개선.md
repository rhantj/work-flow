# 회의록 AI 분석 품질 개선 — 측정과 수정

기간: 2026-08-15 ~ 2026-08-16
브랜치: `feat/meeting-analysis-quality`

---

## 요약 한 줄

"요약과 To-Do가 제대로 안 나온다"는 문제를 고치러 들어갔는데, **문제를 재는 자가 먼저 고장나 있었다.**
계측기를 두 군데 고치고 나서야 진짜 버그 하나가 드러났고, 그 버그가 실제 사용자 피해였다.

---

## 1. 왜 측정부터 했는가

처음 세운 개선 계획은 네 가지였다 — 파서 관대화, 양식 보강, 프롬프트 개선, 음성 분석.
전부 "이게 문제일 것이다"라는 **가설**이었고 근거가 없었다.

고치기 전에 잴 수 있는 상태를 먼저 만들었다. 회의록 12건(8개 시나리오)에 정답을 붙인
평가셋과 자동 채점기를 만들고, provider별로 점수를 냈다.

| 시나리오 | 케이스 수 | 무엇을 보는가 |
|---|---|---|
| 양식 준수 | 2 | 양식대로 적었을 때 |
| 구분자 생략 | 2 | 가운뎃점 없이 자연어로 적었을 때 |
| 우선순위 미체크 | 1 | 체크박스를 안 눌렀을 때 |
| To-Do 다수 | 1 | 실행항목이 6건일 때 |
| 담당자 미지정 | 2 | 담당자를 안 정했을 때 |
| 기한 표현 다양 | 2 | "8/10까지", "배포 당일" 등 |
| 자유 서술 | 1 | 양식을 안 쓰고 줄글로 적었을 때 |
| 환각 유도 | 1 | 막연한 표현을 날짜로 지어내는지 |

채점은 두 갈래다. To-Do는 기계적으로 잰다(정답과 예측을 **근거 문장**으로 짝지어 P/R/F1 +
담당자·기한·우선순위 정확도). 요약은 정답이 하나가 아니라 문자열 대조가 불가능하므로,
케이스마다 체크리스트를 적어두고 LLM에게 항목별 예/아니오만 받는다.

> **왜 근거 문장으로 짝짓는가**: 제목 유사도로 짝지으면, 나중에 제목 다듬는 작업을 할 때
> 채점 결과가 같이 흔들려 개선인지 착시인지 구분할 수 없다. 근거 문장은 원문에서 오므로
> 제목을 어떻게 다듬든 매칭이 흔들리지 않는다.

---

## 2. 측정이 계획을 세 번 뒤집었다

### 2-1. 파서 관대화 — 취소 (고칠 것이 없었다)

계획의 핵심 가설은 "사용자가 `담당 · 내용 · 기한` 형식을 안 지키면 담당자·기한이 유실된다"였다.
`split_todo_cell` 함수만 보면 맞는 말이다:

```python
split_todo_cell("박지수가 알림 중복 원인을 8월 14일까지 파악하기로 했다")
  -> (None, '박지수가 알림 중복 원인을 8월 14일까지 파악하기로 했다', None)
```

그런데 **호출부**가 이미 복원하고 있었다:

```
extract_assignee_candidate(...)                      -> '박지수'
extract_due_date_candidate(...) -> sanitize_due_date -> 2026-08-14
최종 To-Do: 담당='박지수' 기한=2026-08-14 HIGH   (정확)
```

베이스라인도 일치했다 — `구분자 생략` 시나리오의 담당자 정확도 1.00 / 기한 정확도 1.00 으로
`양식 준수`와 완전히 같았다.

**오진의 원인**: 함수 하나만 읽고 그 함수를 부르는 쪽을 읽지 않았다.
그대로 진행했다면 이미 동작하는 경로에 중복 로직을 얹고 회귀 위험만 만들었을 것이다.

### 2-2. HuggingFace 티어가 죽어 있었다

베이스라인에서 HF가 12케이스 **전부 0점**이었다. 러너를 거치지 않고 직접 호출해 예외를 받았다:

```
httpx.HTTPStatusError: Client error '401 Unauthorized'
x-error-message: Invalid username or password.
```

토큰은 `.env`에서 정상 로드됐다(길이·접두사 확인). 즉 요청은 정상이고 서버가 자격증명을 거부했다.
**코드로 고칠 수 있는 문제가 아니라 판단해 고치지 않고 보고했고**, 이후 토큰을 재발급받아 해결됐다.

이 티어는 폴백 체인의 **1순위**다. 즉 그동안 프로덕션은 항상 2순위 이하로 떨어져 돌고 있었다.
아무도 몰랐던 이유는 실패 로그에 예외 타입만 찍혀 401인지 404인지 429인지 구분할 수 없었기
때문이다. HTTP 상태 코드를 로그에 남기도록 보강했다. **응답 본문은 자격증명을 되비칠 수 있어
일부러 남기지 않는다.**

### 2-3. 요약 프롬프트 개선 — 취소 (지표가 아무것도 재고 있지 않았다)

요약 점수가 provider와 거의 무관하게 0.5 언저리에 붙어 있었다. 프롬프트를 고치려다,
그 전에 24개 체크리스트 항목의 심사기 문답을 **전부 열어봤다.**

- 긍정형 문항("~담겼는가") 12개 → **전부 `예`**
- 부재 확인 문항("원문에 없는 날짜나 이름이 요약에 없는가") 12개 → **10개 `아니오`**
- 12/24 = **정확히 0.500.** 측정된 요약 점수와 소수점까지 일치했다.

실패 12건을 원문과 대조한 결과:

| 판정 | 건수 |
|---|---|
| 사실과 다른 실패 | 10 |
| 구조적으로 답할 수 없는 문항 | 1 |
| 애매 | 1 |
| **진짜 결함** | **0** |

가장 뚜렷한 예가 환각 유도 케이스다:

```
요약: 재색인 스크립트 마무리 계획을 논의하고, 다음 주쯤 완료 예정으로 협의
결정사항: 박지수가 재색인 스크립트를 마무리할 예정이다.

Q: '다음 주쯤'이라는 막연한 표현이 특정 날짜로 단정되지 않았는가
A: 아니오  -> 실패
```

모델은 `다음 주쯤`을 **그대로 보존**했고 결정사항에 날짜를 **하나도** 넣지 않았다.
정확히 옳은 동작인데 두 항목 모두 실패로 처리됐다.

거짓 통과도 있었다 — 요약에 `8/10`이 언급조차 안 되는데 "8월 10일로 정확히 반영되는가"에 `예`.

**근본 원인**: 심사기에게 회의록 원문을 한 번도 주지 않고 있었다.

```python
def _build_prompt(summary, decisions, item):
    return (... f"[요약]\n{summary}\n\n[결정사항]\n{...}\n\n[질문]\n{item}")
```

체크리스트 절반이 "**원문에** 없는 날짜나 이름이 ..." 형태인데, 대조할 원문이 없으면
원리상 답할 수 없다. 1.5B 모델과 4B 모델이 **정확히 그 문항에서만** 반복해서 틀린 것이 증거다.
모델을 바꿔도 안 고쳐지는 종류의 문제였다.

---

## 3. 진짜 버그 — 출력이 잘려 분석이 통째로 실패하고 있었다

계측기를 고치는 과정에서, To-Do 6건짜리 회의가 HF·Ollama **양쪽에서** `JSONDecodeError`로
죽는 것을 발견했다. 원인을 실측으로 확정했다:

```
finish_reason: length
completion_tokens: 900          ← 상한값과 정확히 일치
응답 끝: '"keywords": [ "비밀번호 재설정", "스팸", "메일 템플'
```

완성에 **924토큰**이 필요한데 상한이 **900**이었다. 24토큰 차이로 JSON이 문자열 중간에서 끊겼다.

피해가 조용해서 더 나빴다. 잘린 JSON은 파싱에서 죽고 → 폴백이 규칙 기반까지 떨어지고 →
사용자에게는 이런 요약이 나갔다:

> "비밀번호 재설정 메일 개선 회의 내용을 분석해 핵심 결정사항 1건, 업무 후보 6건, 위험요소 1건을 추출했습니다."

내용이 아니라 **개수를 세어 읊는 기계 문구**다. To-Do 1건당 완성 길이가 대략 90토큰씩 늘어나므로,
실사용 회의는 이보다 쉽게 걸린다.

### 조치

| 항목 | 전 | 후 |
|---|---|---|
| HF 출력 상한 | 900 | 2048 |
| Ollama 출력 상한 | 650 | 2048 |
| Ollama 컨텍스트 창 | 4096 | 8192 |

컨텍스트 창을 같이 올린 이유는, 프롬프트만 1,400토큰대라 출력 상한만 올리면 이번엔
**창 쪽에서** 잘리기 때문이다. 한쪽만 고치면 증상이 자리만 옮긴다.

잘림 자체도 로그로 남긴다. 지금까지는 `Unterminated string` 예외만 남아 상한 문제인지
모델이 망가진 것인지 구분할 수 없었다.

---

## 4. 결과

### 4-1. 분석 실패 건수

| 측정 | HF 실패 | Ollama 실패 |
|---|---|---|
| 수정 전 | 1/12 | 1/12 |
| **수정 후** | **0/12** | **0/12** |

`To-Do 다수` 시나리오의 HF F1: **0.00 → 0.67**. 죽어서 0점이던 케이스가 살아났다.

### 4-2. provider별 평균

| provider | To-Do F1 | 요약 |
|---|---|---|
| huggingface | 0.497 | 0.708 |
| ollama | 0.258 | 0.667 |
| rule_based | 0.983 | 0.792 |
| **production** (사용자가 실제로 받는 경로) | **0.983** | **0.708** |

### 4-3. 요약 점수 before/after — **개선이 아니라 교정이다**

| 시나리오 | 전 | 후 |
|---|---|---|
| 환각 유도 | 0.00 | **1.00** |
| 자유 서술 | 0.50 | **1.00** |
| 우선순위 미체크 | 0.50 | **1.00** |
| 구분자 생략 | 0.50 | 0.75 |
| 전체 평균 (production) | 0.500 | 0.708 |

**이 숫자가 올라간 것은 요약이 좋아졌기 때문이 아니다. 채점이 정확해졌기 때문이다.**
요약 프롬프트는 한 글자도 건드리지 않았다. 환각 유도 0.00 → 1.00 은 모델이 원래부터
옳게 동작하고 있었다는 뜻이다.

점수가 오른 것보다 중요한 변화는, **케이스마다 다른 값이 나오기 시작했다**는 것이다.
이전에는 내용과 무관하게 0.5로 수렴했다.

---

## 5. 프로덕션 경로에 대한 이해

provider별 점수를 따로 재는 것만으로는 사용자가 받는 품질을 알 수 없다.
실제 진입점 `_analyze_json_uncached`는 한 겹을 더 씌운다 — **양식 문서면 provider의 To-Do를
버리고 규칙 기반 결과로 덮어쓴다.**

그래서 production 은 이런 합성이다:

- **To-Do**: 양식 문서면 규칙 기반 (provider 무관) → F1 0.983
- **요약·결정사항**: provider 결과 그대로 → HF 값과 일치 (0.708)

유일한 예외가 `자유 서술`이다. 액션 아이템 칸이 없어 덮어쓰기 조건이 성립하지 않고,
To-Do도 provider 결과가 그대로 나간다(production F1 0.80 = HF 0.80).

즉 **양식으로 쓴 회의록의 To-Do 품질은 LLM이 아니라 규칙 기반 로직이 책임진다.**
LLM 교체나 프롬프트 개선으로 그 부분을 올리려는 시도는 효과가 없다.

---

## 6. 양식 문서 보강

실행항목 칸에 `내용 (누가 · 무엇을 · 언제까지)` 헤더만 있고 **예시가 한 줄도 없었다.**
어떤 형식으로 적어야 하는지 양식이 알려주지 못했다.

| 위치 | 추가한 문구 |
|---|---|
| 실행항목 첫 입력 행 | `(예시) 박지수 · 로그인 오류 원인 파악 · 8/20` |
| 실행항목 칸 아래 | `※ 항목이 더 필요하면 표에서 행을 추가하세요.` |
| 참석자 입력 칸 | `(쉼표로 구분: 박지수, 유소은)` |

예시 행은 파서가 `(예시)` **접두사**로 거른다. 문자열 목록에 통째로 넣지 않은 이유는,
사용자가 예시를 지우지 않고 그 아래에 이어서 적는 경우가 흔한데 목록 방식은 문구가
조금만 달라져도 새어나가기 때문이다.

### 함정 하나를 미리 확인했다

예시 행의 **내용 줄만** 걸러지고 그 위 우선순위 체크 줄은 남는다. 이 고아 줄이 다음 행과
짝지어지면 우선순위가 통째로 한 칸씩 밀린다. 실측했다:

```
입력:
  [ ] 긴급  [ ] 보통  [ ] 낮음     ← 예시 행의 고아 체크줄
  [v] 긴급  [ ] 보통  [ ] 낮음
  유소은 · 세션 만료 확인 · 8/22

결과:
  [('HIGH', '유소은 · 세션 만료 확인 · 8/22'), ...]
```

밀리지 않는다. 내용 줄 없이 체크 줄이 이어지면 `pending_priority`를 덮어쓰기 때문이다.

서식은 새로 만들지 않고 기존 파일의 빈 run 에 글자만 채웠다. 사내 서식이라 격자·글꼴을
유지해야 했고, `add_run`으로 새 run 을 만들면 기본 글꼴 크기를 받아 옆 텍스트와 크기가 달라진다.

---

## 7. 남은 한계 — 이 측정을 믿을 때 알아야 할 것

1. **합성 데이터 12건이다.** 절대 점수에 통계적 의미를 두기 어렵다. 개선 판단은 before/after
   차이로만 한다.
2. **`"회의록 원문에 없는 날짜나 이름이 요약에 없는가"` 문항은 원문을 줘도 12건 중 6건에서 틀린다.**
   이중 부정 구조라 4B 모델도 못 읽는다. 문항을 답 가능한 형태로 다시 쓰면 해결되지만,
   골든 데이터를 손대면 되돌리기가 번거로워 이번엔 하지 않았다.
   → **25절에서 해결.** 다만 다시 쓴 문항도 안 됐다. 이 축을 LLM에게 묻는 것 자체가
   문제였고, 지금은 `safety_check` 가 문자열로 답한다.
3. **심사 모델 = HF 분석 모델이다.** huggingface·production 열의 요약 점수는 자기평가다.
   그럼에도 이 편을 택한 이유는 지금 문제가 편향이 아니라 심사기 정확도였기 때문이다
   (사람이 정답을 확인한 22개 항목 기준 1.5B 12건 → 4B 15건, 게다가 1.5B 의 12건은 전부
   긍정형 도장찍기라 실질 변별력이 0이었다).
4. **규칙 기반의 요약 점수(0.792)가 가장 높은 것은 지표의 허점이다.** 규칙 기반 요약은
   날짜와 이름을 아예 쓰지 않아서 "원문에 없는 날짜가 없는가"를 거저 통과한다.
   **아무 말도 안 하면 점수가 오르는 지표다.**
   → **25절에서 해결.** 두 축을 평균하지 않고 곱한다. 규칙 기반은 1위에서 3위로 내려갔다.
5. **평가셋이 문서→섹션 변환(Java `MeetingTemplateParser`)을 타지 않는다.** 픽스처가
   `sections`를 직접 주기 때문이다. 6절의 양식 보강 효과는 이 점수로 측정되지 않고,
   Java 단위 테스트와 수동 실측으로만 검증했다.
6. **Ollama 결과는 실행마다 흔들린다**(온도 0.1에서도 F1 0.317 / 0.267 / 0.258).
   순위는 세 번 모두 안정적이었다.

---

## 8. 배운 것

**함수 하나를 읽고 원인이라고 단정하지 않는다.** 파서 관대화 건은 호출부를 안 읽어서 생긴
오진이었다. 측정이 없었다면 멀쩡한 코드를 고치고 "개선했다"고 적었을 것이다.

**지표가 움직이지 않으면 지표부터 의심한다.** 요약 점수가 provider와 무관하게 0.5에 붙어
있는 것은 "모두 비슷하다"가 아니라 "아무것도 재고 있지 않다"는 신호였다. 집계 숫자만 보면
평생 못 찾고, 항목별 문답을 열어보니 한 번에 보였다.

**조용한 실패가 가장 비싸다.** HF 401도, 출력 잘림도, 폴백이 있어서 서비스는 안 죽었다.
대신 아무도 모르는 채로 품질만 낮은 상태가 유지됐다. 폴백을 넣을 때는 **폴백이 발동했다는
사실 자체를 남기는 것**이 폴백만큼 중요하다.

---

## 부록: 커밋

| 커밋 | 내용 |
|---|---|
| `93d421bc` … `483e3cdf` | 평가셋·채점기·러너·베이스라인 측정 |
| `cfa95169` | HF 실패 로그에 HTTP 상태 코드 기록, 프로덕션 경로 측정 추가 |
| `28607115` | 회의록 양식에 실행항목 작성 예시 삽입 + 파서 필터 |
| `64856285` | 출력 상한에 걸려 잘린 분석 응답 복구 + 잘림 로그 |
| `45a7ff12` | 요약 심사기에 회의록 원문 전달 |

측정 산출물: `output/meeting_eval/` (`production_20260816.csv` / `hf_live_20260816.csv` /
`fixed_20260816.csv` — 각각 HF 죽음·HF 정상·계측기 수정 후)
노트북: `03_meeting_eval_baseline.ipynb`
테스트: FastAPI 82 passed / Spring `MeetingTemplateParserTest` 12 passed

---

# 2부: AI가 실제로 뽑아내고 있었는가 (2026-08-16 추가)

## 9. 던져진 질문

> "저러면 투두를 뽑아주는 게 아니라, 투두를 사람이 회의록에 적고 그걸 AI가 그냥 옮겨주기만 하는 거 아니야?"

맞는 지적이었다. 그리고 1부의 숫자가 그 얘기를 하고 있었는데 내가 짚지 못했다.

**평가셋 12건 중 11건이 실행항목 칸이 채워진 케이스였다.** 그 경로는
`_analyze_json_uncached` 가 LLM 의 To-Do 를 버리고 규칙 기반으로 덮어쓴다. 즉 1부의
F1 0.983 은 "AI 가 뽑은 품질"이 아니라 **"사람이 적은 것을 옮겨 적은 품질"**이다.

AI 가 회의 내용에서 할 일 자체를 판단하는 능력은 자유 서술 1건으로만 쟀고, 그 점수는 0.80 이었다.

## 10. 추출 전용 평가셋을 만들었다

실행항목 칸을 비운 케이스 18건. 칸이 비면 덮어쓰기 조건(`if not action_items: return result`)에
걸리지 않아 LLM 결과가 그대로 나간다.

**정답이 0건인 케이스를 6건 넣었다.** 추출에서 가장 중요한 것은 없는 할 일을 지어내지
않는 것인데, 기존 평가셋에는 그것을 재는 케이스가 하나도 없었다.

채점기도 고쳤다. 예전에는 정답 0건일 때 **아무것도 안 만든 경우와 3건을 지어낸 경우가
둘 다 f1=0.0** 이라 서로 구분되지 않았다. 즉 "환각을 안 했다"를 점수로 증명할 방법이 없었다.

## 11. 첫 측정에서 드러난 것 — 지어내고 있었다

F1 0.600. 그런데 손실의 위치가 뚜렷했다.

| | 케이스 | F1 | 재현율 |
|---|---|---|---|
| 진짜 할 일이 있는 회의 | 7건 | 0.86 | 1.00 |
| 할 일이 없는 회의 | 3건 | 0.00 | — |

**재현율이 전부 1.00 이다. 진짜 할 일은 한 번도 놓치지 않았다.** 손실은 전부 없는 것을
만들어내는 쪽이었다. 실제로 사용자에게 나가던 것들:

지표 공유 회의(배포 성공률 98% 보고, 조치 사항 없음)에서:

```
- '회의록 AI 분석 API 구현'                 HIGH
- '분석 결과 화면과 업무 보드 등록 흐름 연결'  HIGH
- '팀장 검토용 To-Do 승인 화면 점검'         MEDIUM
```

회의와 아무 관련이 없다. `app/main.py` 에 **하드코딩된 목록**이었다.

제안이 기각된 회의("이번 분기에는 안 하는 걸로 하시죠")에서:

```
- '알'                  담당=유소은  HIGH     ← '알겠습니다'가 제목으로 다듬어지다 잘린 것
- '나중에 다시 이야기'   담당=유소은  HIGH
```

## 12. 원인 세 가지와 수정

**(1) 하드코딩된 가짜 To-Do 목록.** 업무 문장을 못 찾으면 이 서비스 자체의 기능 3개를
채워 사용자 회의록에서 뽑은 것처럼 내보냈다. 제거했다.

**(2) 모델의 빈 결과를 코드가 덮어썼다.**

```python
if not explicit_candidates:
    return todos or build_todos(...)   # 빈 결과 -> 규칙 기반으로 대체
```

모델이 "할 일 없음"이라고 **옳게** 답해도 코드가 그 정답을 지웠다. 원문에 업무 선언
문장이 하나도 없으면 빈 결과가 곧 판단이므로 그대로 둔다.

**(3) "겠습니다"가 들어가면 무조건 업무 선언으로 봤다.** 그래서 "알겠습니다"가 업무가 됐다.
관측된 실패에서 나온 표현만 부정 목록에 넣었다 — 추측으로 넓히면 진짜 업무를 걸러낸다.

**결과: F1 0.600 → 0.900. 정답 0건 케이스 3건 모두 지어내지 않음.**
기존 12건은 0.983 그대로로 회귀 없음.

## 13. 문체 지적 — 두 번째 계측 오류

> "근데 문서 회의록에는 ~겠습니다 이렇게 안 적잖아. 음성본에는 그럴 수 있어도"

이것도 맞았다. 추출 케이스 10건이 전부 `"박지수: 제가 ~하겠습니다"` 구어체였다.
그건 음성 전사 문체이고, 타이핑하는 문서 회의록의 문체가 아니다.
구어체만으로 재면 **"겠습니다"에 기대는 규칙 기반 경로를 과대 대표**하게 된다.

개조식·서술형 8건을 추가해 다시 쟀다.

| 문체 | 건수 | F1 | 재현율 | 손실 방향 |
|---|---|---|---|---|
| 구어체 (음성 전사형) | 10 | 0.900 | 0.975 | **과다 생성** |
| 문서형 (개조식·서술형) | 8 | 0.750 | 0.750 | **누락** |

**실패 방식이 정반대다.** 정답 0건 케이스는 양쪽 합쳐 6건 모두 지어내지 않았다 —
환각 수정은 문체와 무관하게 작동한다.

문서형이 놓친 2건은 조건이 같다. **담당자나 기한이 없는 항목**이다.

- `doc-noassign-01`: "배포 스크립트 점검은 진행하되 담당자는 다음 회의에서 정한다" → 0건
- `doc-vague-01`: "완료 시점은 다음 주 중으로 예상되나 확정하지 못함" → 0건

이 누락은 이번 수정이 만든 회귀가 아니다. 예전 폴백도 `doc-vague-01` 은 0건이었고,
`doc-noassign-01` 은 1건 냈지만 제목이 `'회의내용 - 배포 스크립트가 간헐적으로 중단…'`
처럼 섹션 라벨까지 들어간 결과였다.

프롬프트를 보면 원인이 짐작된다. 담당자·기한 규칙은 "명시적일 때만 적어라"로 엄격한데,
**담당자나 기한 없이도 할 일은 할 일이라는 지시가 어디에도 없다.**

## 14. 그래서 답은

**양식대로 실행항목 칸을 채우면** — 그건 옮겨 적기가 맞다. AI 는 날짜 변환·이름 매칭·
분류만 한다. F1 0.983 은 그 작업의 점수다.

**칸을 비우면** — AI 가 실제로 판단한다. 문서형 0.750 / 구어체 0.900, 진짜 할 일은
거의 놓치지 않고(재현율 0.75~0.98), 할 일이 없는 회의에서는 이제 아무것도 만들지 않는다.

두 경로가 한 기능 안에 섞여 있었고, 어느 쪽이 동작 중인지 숫자만 봐서는 알 수 없었다.
그것을 나눠 잰 것이 2부의 성과다.

## 15. 2부에서 배운 것

**"AI 기능"이라고 부르는 것 안에 AI 가 없을 수 있다.** 덮어쓰기 한 줄 때문에 양식 문서의
To-Do 는 전부 규칙 기반이 만들고 있었다. 평가셋이 그 경로로 기울어 있어 숫자로도 안 보였다.

**평가셋의 문체가 곧 측정 대상이다.** 구어체로만 짜서 규칙 기반 경로를 과대 대표했다.
같은 회의를 세 문체로 넣어보니 결과가 갈렸다.

**폴백은 정답도 지운다.** 1부의 교훈이 "조용한 실패가 비싸다"였다면, 2부는 그 반대편이다.
`todos or build_todos(...)` 는 실패를 메우려던 코드인데, 모델이 맞게 답한 경우까지 덮어썼다.
폴백을 쓸 때는 **무엇을 실패로 볼 것인지**를 명시해야 한다. 빈 결과는 실패가 아닐 수 있다.

## 부록: 2부 커밋

| 커밋 | 내용 |
|---|---|
| `f4f0262e` | 추출 평가셋 10건 + 채점기가 환각을 구분하도록 수정 |
| `6c5a78bd` | 하드코딩 목록 제거, 빈 결과 존중, 대화 응답 제외 |
| `9d8c2827` | 문서형 회의록 8건 추가 |

평가셋: `App/backend_fastapi/tests/fixtures/meeting_eval_extraction/` (18건, 정답 To-Do 23건)
테스트: FastAPI 93 passed

---

# 3부: 문서형 누락을 메웠다 (2026-08-17 추가)

## 16. 2부가 남긴 숙제

2부의 마지막 표에서 문서형 회의록의 실패 방향은 **누락**이었다(F1 0.750 / 재현율 0.750).
놓친 2건은 조건이 같았다 — **담당자나 기한이 없는 항목**이다.

그때 프롬프트를 읽고 짐작만 해뒀던 원인을 이번에 모델 응답으로 확인했다.

```
doc-noassign-01  "배포 스크립트 점검은 진행하되 담당자는 다음 회의에서 정한다"
  -> todos: []
doc-vague-01     "재색인 스크립트는 계속 진행하되 완료 날짜는 확정하지 않는다"
  -> todos: []
```

두 건 다 모델이 **빈 배열을 반환**했다. 파싱이나 후처리에서 사라진 것이 아니라
모델이 애초에 할 일로 보지 않았다.

프롬프트에는 담당자 규칙 5개, 마감일 규칙 4개가 있는데 전부 "명시적일 때만 적어라"는
**제약**이다. **무엇을 할 일로 볼 것인가**를 정하는 지시는 한 줄도 없었다.
모델이 담당자·기한 규칙을 To-Do 선정 기준으로 확대 해석한 셈이다.

## 17. 측정 도중에 계측기 결함이 하나 더 나왔다

첫 재현 측정에서 `doc-multi-01`(정답 4건)이 갑자기 0점이 나왔다. 프롬프트를 고치기 전이라
2부 기록(0.750)과도 어긋났다. 모델에 같은 입력을 직접 넣어보니 4건을 정확히 뽑아냈다.

원인은 **HF 호출의 일시 실패**였다. 실패하면 Ollama, 그것도 실패하면 규칙 기반으로
폴백하는데, 폴백은 경고 로그만 남기고 점수에는 아무 표시 없이 섞여 들어간다.
그날의 네트워크 사정이 그대로 "품질 개선/악화"로 읽히는 구조였다.

평가 스크립트가 폴백 경고를 잡아 케이스별로 표시하게 고쳤다. 이후 측정에서
`ext-*` 3건이 폴백으로 잡혔고(HF `RemoteProtocolError` -> Ollama `ReadTimeout`),
그 값은 신뢰 구간 밖으로 두고 재측정했다.

> 1부에서 고친 계측기가 2개, 2부에서 1개, 3부에서 1개다. **측정값이 예상과 다르면
> 대상보다 계측기를 먼저 의심하는 편이 결과적으로 빨랐다.**

## 18. 프롬프트에 넣은 것

`build_ollama_prompt` 에 `todos 선정 규칙` 블록을 추가했다. 요지는 세 가지다.

1. 하기로 정해진 일은 **담당자나 기한이 없어도** todos에 넣는다. 모르는 칸은
   `assignee_candidate: ""`, `due_date: null` 로 두고 업무 자체는 남긴다.
2. 이미 시작된 일도 **남은 작업이 있고 계속하기로 했으면** 할 일이다.
3. 제외 대상을 명시한다 — 남은 작업 없이 끝난 일의 완료 보고, 조치가 결정되지 않은
   지표·현황 공유, 하지 않기로 하거나 보류한 일, 의견·질문·맞장구.

3번을 함께 넣은 이유는 1·2번만 넣으면 2부에서 잡은 환각이 되살아나기 때문이다.
"더 뽑아라"는 지시는 "없는 것도 뽑아라"로 새기 쉽다.

**예시 문장은 픽스처에서 가져오지 않았다.** 평가셋 문장을 프롬프트에 넣으면 점수는
오르지만 그건 답을 알려주고 시험을 보는 것이다. `"OO 작업은 ..."` 형태의 뼈대만 남겼다.

### 두 번째 조정

1차 수정 후 `doc-vague-01` 이 여전히 0건이었다. 이 회의의 제목이 `재색인 스크립트 진행 상황`
이라 모델이 제외 규칙의 "현황 공유"로 분류한 것으로 보인다. 제외 조건을
`남은 작업 없이 끝난 일` / `별도 조치가 결정되지 않은 현황 공유` 로 좁히고,
"이미 시작된 일도 잔여 작업이 있으면 할 일"이라는 규칙(2번)을 따로 세우자 해결됐다.

## 19. 결과

| 문체 | 건수 | F1 (전) | F1 (후) | 재현율 (전) | 재현율 (후) |
|---|---|---|---|---|---|
| 문서형 (개조식·서술형) | 8 | 0.750 | **0.875** | 0.750 | **0.875** |
| 구어체 (음성 전사형) | 10 | 0.900 | 0.900 | 0.975 | 0.975 |
| 전체 | 18 | 0.833 | **0.889** | 0.847 | **0.931** |

- **정답 0건 케이스 6건, 여전히 하나도 지어내지 않았다.** 환각 수정은 유지됐다.
- 기존 12건 평가셋 F1 **0.983 그대로** — 양식 경로 회귀 없음.
- 폴백 0건 상태에서 2회 측정, 같은 수치가 나왔다.
- 테스트: FastAPI 134 passed.

## 20. 남은 1건은 고치지 않았다

`doc-noassign-01` 은 이제 할 일을 뽑는다. 담당자는 빈칸, 기한은 null 로 정확하다.
그런데 점수는 0점이다.

```
정답  "배포 스크립트 점검"        근거: "배포 스크립트 점검은 진행하되"   (결정사항)
예측  "배포 스크립트 중단 현상 분석"  근거: "…간헐적으로 중단되는 현상 보고"  (회의내용)
```

같은 일을 가리키지만 **근거 문장이 다르다.** 채점기는 근거로 짝짓기 때문에 미매칭이다.
"결정 사항 쪽 문장을 근거로 써라"는 규칙을 넣어봤지만 이 건에서는 먹히지 않았다.

여기서 멈췄다. 더 밀어붙이면 **평가셋 한 건에 맞추는 튜닝**이 된다. 원래 결함
(담당자 없으면 아예 안 뽑음)은 고쳐졌고, 남은 것은 표현 선택 차이다.
문서형 0.875 는 "8건 중 7건 정확, 1건은 업무는 찾았으나 근거가 달라 미매칭"으로 읽어야 한다.

## 21. 확인이 필요한 정답 결함

`doc-vague-01` 의 정답 담당자가 `박지수` 인데 **회의록 원문에 그 이름이 없다.**
참석자 목록에만 있다. 프롬프트 규칙 3(`참석자 목록에 있다는 이유만으로 임의 배정 금지`)과
정면으로 충돌하는 정답이다. 모델이 규칙을 지키면 이 항목의 담당자 정확도는 영원히 0이다.

F1 에는 영향이 없어(담당자 정확도는 매칭된 쌍에서만 계산) 이번 수치는 그대로 유효하다.
정답을 `""` 로 고치는 것이 맞아 보이지만, **정답 수정은 점수를 올리는 방향으로 쓰이면
안 되므로** 임의로 바꾸지 않고 남겨둔다.

## 부록: 3부 커밋

| 커밋 | 내용 |
|---|---|
| (이 변경) | todos 선정 규칙 추가 — 담당자·기한 없는 할 일을 살리고 제외 조건을 명시 |

측정 스크립트는 세션 스크래치패드에 있다(`run_extraction_eval.py`, 폴백 표시 포함).

---

## 22. 이어서 — 폴백이 좋은 결과를 버리고 있었다

3부의 표에서 구어체가 0.900 에서 안 오르는 이유를 마저 봤다. 손실은 **과다 생성**이었는데,
지어낸 주체가 모델이 아니었다.

`repair_ollama_todos` 는 모델 결과를 네 조건에서 규칙 기반으로 **통째 교체**한다.
그중 하나가 `missing_explicit_tasks` — 정규식이 센 "업무 선언 문장" 수보다 모델의
To-Do 가 적으면 누락으로 보는 조건이다.

어느 조건이 걸리는지 케이스별로 찍어봤다.

| 케이스 | 걸린 조건 | 모델이 뽑은 수 | 정답 수 | 교체 후 예측 수 | F1 |
|---|---|---|---|---|---|
| `ext-mixed-01` | 선언수부족 | 2 | 2 | 4 | 0.667 |
| `ext-multi-01` | 선언수부족 | 4 | 4 | 5 | 0.667 |
| `ext-request-01` | 선언수부족 | 1 | 1 | 2 | 0.667 |
| `freeform-01` (기존 셋) | 선언수부족 | 2 | 2 | 3 | 0.800 |

**모델은 네 건 모두 정답 개수와 정확히 같게 뽑았다.** 그걸 버리고 규칙 기반으로 바꾼
결과가 이런 제목이다.

```
ext-multi-01   "템플릿을 8/18까지"  "8/19까지 보"  "재발송 제한 로직을 8/21까지 넣"
ext-mixed-01   "재현해 보"  "8/19까지 확인"
```

같은 회의를 문서형으로 바꾼 `doc-multi-01` 은 이 조건에 안 걸려
`메일 템플릿 정리 / 발신 도메인 인증 설정 확인` 처럼 멀쩡히 나온다.
**즉 구어체가 문서형보다 나빴던 게 아니라, 구어체에서만 폴백이 걸리고 있었다.**
"겠습니다" 같은 선언 표현이 구어체에만 있으니 정규식이 거기서만 문장을 세기 때문이다.

조건 자체가 조잡했다. 정규식이 세는 문장에는 **문제 보고나 요청 발언도 섞인다.**
`ext-request-01` 의 "…절차가 빠져 있습니다"가 선언 문장으로 세어져, 할 일 1건을 뽑은
정확한 결과가 "누락"으로 판정됐다.

### 결과

조건을 껐다 켰다 하며 두 평가셋을 다 쟀다.

| 평가셋 | 켜짐(기존) | 꺼짐 |
|---|---|---|
| 추출셋 구어체 10건 | 0.900 | **1.000** |
| 추출셋 문서형 8건 | 0.875 | 0.875 |
| 추출셋 전체 18건 | 0.889 | **0.944** |
| 기존 12건 | 0.983 | **1.000** |

**이 조건이 걸린 4건은 전부 손해였고, 이득 본 케이스는 하나도 없었다.** 조건을 제거했다.

안전망은 남는다 — 모델이 빈 결과를 냈거나(`not todos`), 스키마 예시 문구를 그대로
베꼈거나(`has_placeholder`), 전원 미배정이면(`all_unassigned`) 여전히 규칙 기반으로 간다.

### 2부의 교훈이 한 번 더

2부에서 걷어낸 `todos or build_todos(...)` 와 같은 종류다. 그때는 **빈 결과**를 실패로
오인했고, 이번에는 **개수가 적은 결과**를 실패로 오인했다.

> 폴백을 쓸 때는 무엇을 실패로 볼 것인지를 명시해야 한다.

두 번 다 판정 기준이 실제 실패와 어긋나 있었고, 두 번 다 **정답을 지우는 방향**으로
틀렸다. 폴백은 조용히 동작하므로 이런 오판은 지표를 따로 만들기 전까지 드러나지 않는다.

### 현재 수치

| 구분 | 2부 끝 | 3부 끝 |
|---|---|---|
| 추출 구어체 10건 | 0.900 | **1.000** |
| 추출 문서형 8건 | 0.750 | **0.875** |
| 추출 전체 18건 | 0.833 | **0.944** |
| 기존 12건 | 0.983 | **1.000** |
| 정답 0건 6건 중 환각 | 0건 | 0건 |

테스트: FastAPI 135 passed.

---

## 23. 양식을 4열로 바꿨다 — 추출기가 정한 제약

실행항목 표를 2열에서 4열(실행 항목 · 담당자 · 완료 기한 · 우선순위)로 바꿨다.
체크박스를 누르는 대신 값을 적고, 담당자·기한을 가운뎃점으로 이어 적지 않아도 된다.

바꾸기 전에 docx→텍스트 경로부터 확인했다. **거기서 이번 설계가 결정됐다.**

### 추출기가 표 구조를 버린다

Spring 은 POI 를 쓰지 않는다. `MeetingAnalysisService.extractDocxTextFromBytes` 가
ZIP 을 열어 `word/document.xml` 을 정규식으로 훑는다. 표를 아는 코드는 한 줄도 없고,
`<w:p>` 를 개행으로 바꾸는 부수효과로 **셀마다 한 줄**이 나온다. 그리고 결정적으로

> **빈 셀은 빈 줄조차 남기지 않고 통째로 사라진다.**

`<w:tc><w:p></w:p></w:tc>` 가 개행과 공백만 내놓고, 뒤이은 `\n{2,}` 축약이 그걸 지운다.

그래서 **열 위치로는 어느 칸인지 알 수 없다.** 4열 표에서 담당자를 비우면 그 행은
네 줄이 아니라 세 줄로 나오고, 기한이 담당자 자리로 밀린다.

구양식이 이 문제를 안 겪은 이유는 `[ ] 긴급 [ ] 보통 [ ] 낮음` 체크줄이 **정규식으로
알아볼 수 있는 행 경계 표시**였기 때문이다. 우연히 안전했던 것이지 설계된 안전은 아니었다.

### 그래서 우선순위 값이 행의 끝이다

새 양식은 빈 행마다 우선순위 칸에 **"보통"을 미리 인쇄**한다. 파서는 긴급/보통/낮음 토큰을
만나면 한 행이 끝난 것으로 보고, 그 앞에 쌓인 줄들을 이렇게 가른다.

- 첫 줄 → 실행 항목 (이 칸이 비면 행 자체를 안 쓴 것이다)
- 나머지 → 기한처럼 보이면 기한, 아니면 담당자

담당자로 잘못 읽힌 값은 참석자 대조에서 걸러지므로, 최악의 경우 기한을 놓칠 뿐
엉뚱한 사람에게 배정되지는 않는다.

양식 안내에 "그대로 두면 보통으로 처리되니 지우지 마세요"를 넣은 것은 문구가 아니라
**파싱 제약**이다. 그 값을 지우면 그 행이 다음 행과 합쳐진다.

### 빈 양식을 그대로 올리면

실제 docx 를 Spring 추출기와 같은 방식으로 텍스트화해 끝까지 태워봤다. 여기서
설계 결함이 하나 나왔다.

표 안에 예시 행을 두면 4열이라 예시가 네 칸으로 흩어지는데, 예시를 걸러내는
`(예시)` 접두사 규칙은 **첫 칸만** 잡는다. 나머지 세 칸이 살아남아 —

```
=== 보일러플레이트 제거 후 실행항목 ===
['박지수', '8/20', '긴급', '보통', '보통', '보통', '보통']
=== parse_action_items ===
  ActionItem(priority='HIGH', content='박지수', assignee='', due_date='8/20')
```

담당자 이름이 업무 제목이 된 유령 To-Do다. 예시를 표 밖 안내 줄(`※ 예: …`)로 옮겨
해결했다. ※ 로 시작하는 줄은 이미 걸러진다. 지금은 빈 양식을 그대로 올리면 To-Do 0건이다.

### 검증

| 대상 | 결과 |
|---|---|
| 사용자가 채운 신양식 (실제 추출 경로 왕복) | 3건 전부 담당자·기한·우선순위 정확 |
| 손대지 않은 빈 양식 | To-Do 0건 |
| 구양식 문서 | 계속 동작 (체크박스 경로 유지) |
| 기존 12건 평가셋 | F1 1.000 유지 |
| 테스트 | FastAPI 138 passed / Spring 회의록 184 passed |

구양식 호환을 남긴 이유는 이미 그 양식으로 쓴 회의록이 있기 때문이다. 파서가 먼저
체크줄이 있는지 보고 없으면 신양식으로 읽는다.

### 남은 것

- 평가셋에 신양식 케이스가 없다. 지금 검증은 단위 테스트와 추출 왕복 확인이고,
  품질 점수로는 재지 않았다. 신양식 픽스처를 넣으면 수치로도 볼 수 있다.
- 저장소에 docx→텍스트 구현이 **세 개**다(Spring ZIP+정규식 / FastAPI python-docx /
  프론트 미리보기). 셋의 평탄화 규칙이 다르다 — Spring 은 셀당 한 줄, FastAPI 는 행당 한 줄,
  프론트는 빈 줄을 남긴다. 한쪽에 맞춘 파서는 다른 쪽 입력을 잘못 읽는다.
  지금 분석 경로는 Spring 것이라 거기 맞췄다.

---

## 24. 신양식을 수치로 재봤다 — 이득은 안 보였다

23절에서 양식을 4열로 바꾸고 "품질 점수로는 안 쟀다"를 한계로 남겼다. 그걸 쟀다.

기존 12건을 **내용과 정답은 그대로 두고 실행항목 형식만** 신양식으로 옮겼다(11건,
`freeform-01` 은 실행항목 칸이 비어 있어 제외). 그래야 점수 차이가 양식 때문인지
다른 요인 때문인지 갈린다.

```
구양식  [v] 긴급  [ ] 보통  [ ] 낮음        신양식  알림 중복 원인 문서화
        박지수 · 알림 중복 원인 문서화 · 8/10까지        박지수
                                                        8/10까지
                                                        긴급
```

### 결과

| 지표 | 구양식 11건 | 신양식 11건 |
|---|---|---|
| To-Do F1 | 1.000 | 1.000 |
| 담당자 정확도 | 1.000 | 1.000 |
| 기한 정확도 | 1.000 | 1.000 |

**11건 전부, 세 지표 전부 동일하다.** 케이스 단위로 봐도 오르내린 항목이 하나도 없다.

### 이걸 어떻게 읽어야 하나

**이 평가셋으로는 양식 변경의 이득을 잴 수 없다.** 구양식이 이미 만점이라 오를 자리가
없다. 22절에서 폴백을 걷어내며 구양식 점수가 0.983 -> 1.000 이 된 직후라 더 그렇다.

그러니 결론은 "신양식이 더 좋다"가 아니라 **"신양식이 구양식만큼은 한다"** 이다.
회귀가 없다는 것 — 그게 이 측정이 증명한 전부다. 그것도 값은 정보다. 파서를 새로 짰고
양식 파일을 갈아엎었으니, 조용히 나빠지지 않았다는 확인이 필요했다.

양식을 바꾼 진짜 이유는 점수가 아니었다.

- 체크박스에 v 를 넣는 수고가 없어진다
- 가운뎃점 두 개로 세 칸을 나누던 규칙이 사라진다. 그 규칙은 어기기 쉽고, 어기면
  담당자가 업무 제목에 박혔다(`2026-08-04` 트러블슈팅의 뿌리가 그것이었다)
- 담당자·기한이 각자 칸을 가지므로 문장에서 유추하지 않아도 된다

셋 다 **작성자 경험**이지 모델 성능이 아니다. 지표가 그걸 보여주지 못하는 게 맞다.

### 무엇을 더 재야 이득이 보이나

구양식이 실제로 깨지는 입력이 평가셋에 없다. 예를 들면 —

- 칸 순서를 바꿔 적은 경우 (`업무 · 담당자 · 기한`)
- 가운뎃점을 하나만 쓰거나 세 개 쓴 경우
- 담당자 칸에 두 사람을 적은 경우

이런 케이스를 넣으면 두 양식의 차이가 수치로 드러날 것이다. 다만 그건 **신양식이 이기도록
설계한 케이스**이기도 하다. 넣는다면 그 의도를 문서에 적어두고 넣어야 한다.
지금은 넣지 않았다.

### 회귀 방어

두 셋의 정답이 갈라지면 나란히 비교할 수 없다. `test_golden_answers_match_the_old_format_set`
이 그걸 막는다. 한쪽 픽스처만 고치면 테스트가 깨진다.

평가셋: `App/backend_fastapi/tests/fixtures/meeting_eval_v2/` (11건)
테스트: FastAPI 142 passed

---

## 25. 요약 지표를 고쳤다 — 두 번 틀리고 세 번째에

7절 한계 4번에 이렇게 적어뒀다. **"규칙 기반의 요약 점수(0.792)가 가장 높은 것은 지표의
허점이다. 아무 말도 안 하면 점수가 오르는 지표다."** 그걸 고쳤다.

### 25-1. 왜 아무 말도 안 하면 점수가 올랐나

케이스마다 문항이 딱 2개, 반반이었다.

```
[충실도] 임베딩 모델 교체와 재색인 결정이 요약에 담겼는가
[안전성] 회의록 원문에 없는 날짜나 이름이 요약에 없는가
```

이 둘을 그냥 평균했다. **두 축은 실패 방향이 반대라 평균하면 서로를 가려준다.**
날짜와 이름을 아예 안 쓰는 규칙 기반 요약은 안전성을 거저 통과하고, 충실도 한 문항만
잃는다. 바닥값이 0.5다.

**고친 방식: 두 축을 따로 세고 곱한다.**

| 충실도 | 안전성 | 종합 | 뜻 |
|---|---|---|---|
| 1.0 | 1.0 | **1.00** | 담았고 지어내지 않았다 |
| 1.0 | 0.0 | **0.00** | 지어냈으면 아무리 잘 담아도 못 쓴다 |
| 0.0 | 1.0 | **0.00** | 아무 말도 안 한 것은 안전한 게 아니다 |

기본값을 일부러 비대칭으로 뒀다. 충실도 문항이 없으면 0.0, 안전성 문항이 없으면 1.0이다.
**잰 것이 없으면 점수를 줄 근거가 없지만(충실도), 걸어둔 제약이 없으면 어긴 것도
없다(안전성).** 반대로 두면 문항을 빠뜨린 케이스가 조용히 만점을 받는다.

### 25-2. 문항을 유형별로 갈라놓고 보니 두 케이스는 충실도를 아예 안 재고 있었다

`duedate-02` 와 `halluc-01` 은 두 문항이 **모두 안전성**이었다.

```
duedate-02  '배포 당일'이라는 표현이 특정 날짜로 지어내지지 않고 미확정으로 남는가   <- 안전성
            회의록 원문에 없는 날짜나 이름이 요약에 없는가                          <- 안전성
```

문면만 보면 첫 문항은 "담겼는가"를 묻는 것처럼 읽힌다. 그래서 아무도 눈치채지 못했다.
곱셈으로 바꾸면 이 두 케이스는 **구조적으로 항상 0점**이 된다. 충실도 문항을 새로 세웠다.

이런 게 다시 생기지 않도록 픽스처 불변조건 테스트를 걸었다
(`test_summary_checklist_shape.py`). 모든 케이스가 충실도 문항을 최소 하나 가져야 한다.

### 25-3. 안전성 문항은 두 번 고쳐도 안 됐다

여기서부터가 이번 작업의 절반이다.

**1차 시도.** 이중 부정(`없는 것이 없는가`)을 긍정형으로 바꿨다. `요약에 적힌 날짜와
사람 이름이 모두 회의록 원문에 나오는가`. 재측정했더니 규칙 기반 요약이 안전성 0.417.
**지어낼 재료가 없는 요약이 환각 문항에서 떨어진다.** 앞뒤가 안 맞았다.

**2차 시도.** 대조 입력 세 개로 문구를 골랐다. `나오는가` → `찾을 수 있는가` 로 바꾸자
세 경우를 다 맞혔다. 적용 후 ollama 0.500→0.833, rule_based 0.417→0.583. 그런데
**HF는 0.417 그대로**였다.

**원인.** 같은 요약 문장에 원문만 갈아 끼워봤다.

| 요약 (날짜·이름 없음) | 원문 | 답 |
|---|---|---|
| `알림 중복 발송 원인을 파악하고…` | 짧은 합성 원문 | 예 |
| `알림 중복 발송 원인을 파악하고…` | 실제 `nosep-01` 원문 | **아니오** |

**원문에 날짜·이름이 많으면 심사기가 대조 방향을 뒤집어 읽는다.** "원문엔 있는데 요약엔
없네"로 판단한다. 요약→원문으로 물었는데 원문→요약으로 답한다. 문구를 아무리 다듬어도
원문 구성에 따라 뒤집히니, LLM 문답으로 재는 한 재현되지 않는다.

**3차 — 질문을 바꾸는 대신 질문하는 상대를 바꿨다.**

"지어낸 날짜가 있는가"는 **판단이 아니라 문자열 대조다.** LLM에게 물을 일이 아니었다.
`meeting_eval/safety_check.py` 가 정규식으로 답한다.

- 요약과 원문에서 날짜를 뽑아 `(월, 일)`로 정규화하고 집합 차를 본다
- 표기가 갈려도(`8월 10일` / `8/10` / `2026-08-10`) 같은 날이면 같은 값이다
- 지어낸 날짜가 하나라도 있으면 0

체크리스트에는 판단이 필요한 문항만 남겼다(`'다음 주쯤'이 특정 날짜로 바뀌지 않고
남아 있는가` 등). 이 문항들은 측정에서 정확히 작동했다.

**사람 이름은 재지 않기로 했다.** 성씨 목록으로 뽑아보니 `정리해`, `문서로`, `남기기`가
이름으로 잡혔다. 형태소 분석 없이는 안 된다. 담당자 환각이라는 실제 위험은
`assignee_accuracy` 가 구조화된 필드로 이미 정확히 재고 있다. **재는 척하는 것이
안 재는 것보다 나쁘다.**

### 25-4. 결과

12건, 심사 모델은 HF 하나로 고정(분석 provider만 바꿈).

| provider | 충실도 | 안전성 | 종합 | 종전 요약 점수 |
|---|---|---|---|---|
| huggingface | **1.000** | 1.000 | **1.000** | 0.708 |
| **auto** (사용자가 받는 경로) | **1.000** | 1.000 | **1.000** | 0.708 |
| rule_based | 0.917 | 1.000 | 0.917 | **0.792 (1위)** |
| ollama | 0.833 | 1.000 | 0.833 | 0.667 |

**규칙 기반이 1위에서 3위로 내려갔다.** 목표한 것이 이것이다.

### 25-5. 이 수치를 믿을 때 알아야 할 것

**종전 열과 나란히 놓고 "요약이 좋아졌다"고 읽으면 안 된다.** 요약은 한 글자도 안 고쳤다.
문항 문구와 집계 방식이 바뀐 것이고, 이건 **개선이 아니라 교정이다**(4-3절과 같다).

**안전성 1.000 은 변별력이 없다는 뜻이기도 하다.** 12건에서 실제로 아무도 날짜를
지어내지 않았다. 환각 유도 케이스인 `halluc-01` 조차 걸려들지 않았다. 검사가 작동한다는
증거는 단위 테스트 7건뿐이고, 평가셋으로는 증명되지 않았다. 다만 종전의 0.417 같은
값은 **거짓 신호**였고 지금은 참인 값이라, 못 가르는 것과 틀리게 가르는 것은 다르다.

**충실도 축은 아직 관대하다.** 12건 중 갈린 것이 3건뿐이다.

```
ollama      freeform-01    "회의록 분석 폴백 논의 내용을 분석해 핵심 결정사항 2건, 업무 후보 3건, 위험요소 2건을 추출했습니다."
ollama      unassigned-01  "OCI 배포 스크립트 점검 회의"
rule_based  unassigned-01  "OCI 배포 스크립트 점검에 대한 회의"
```

`"OCI 배포 스크립트 점검 회의"` 같은 요약이 나머지 11건에서는 충실도 1.00을 받는다.
**다음에 고칠 것은 이쪽이다.** 다만 충실도는 판단 문제라 안전성처럼 문자열 검사로
옮길 수 없다.

**심사 모델이 곧 분석 모델이다.** huggingface·auto 열은 자기평가다(7절 3번과 동일).

### 25-6. 남긴 것

- `meeting_eval/safety_check.py` — 지어낸 날짜 검사(결정적, LLM 호출 없음)
- `meeting_eval/summary_judge.py` — 두 축 분리, `충실도 × 안전성`
- `meeting_eval/dataset.py` — 문항 유형(`kind`) 누락을 로드 시점에 거부
- `meeting_eval/runner.py` — 리포트에 두 축과 `invented_dates` 를 함께 실음
- 픽스처 3셋 41건 — 문항에 유형 표기, 못 쓰는 안전성 문항 제거
- 트러블슈팅: `docs/trouble-shooting/2026-08-17-요약-안전성-문항이-원문에-따라-답을-뒤집은-문제.md`

테스트: FastAPI 159 passed

---

## 26. 충실도 축이 관대했던 이유는 문항 수였다

25절 끝에 "다음에 고칠 것은 이쪽"이라고 적어둔 걸 처리했다. 12건 중 3건만 갈리고,
`"OCI 배포 스크립트 점검 회의"` 같은 껍데기 요약이 나머지 11건에서 충실도 1.00을 받던 문제다.

### 26-1. 원인은 심사 모델이 아니라 문항 설계였다

처음에는 심사 모델을 키우는 쪽을 생각했다. 그런데 문항을 세어보니 원인이 다른 데 있었다.

```
duedate-01      coverage 1개
formal-01       coverage 1개
many-01         coverage 1개   ← 정답 To-Do 가 6건인데 문항은 1개
...
12건 중 9건이 coverage 문항 1개
```

문항이 하나면 그 하나만 통과해도 충실도가 만점이다. 게다가 그 하나가 복합문이었다.

| 종전 문항 | 문제 |
|---|---|
| `임베딩 모델 교체와 재색인 결정이 요약에 담겼는가` | 둘 중 하나만 담아도 "예" |
| `담당자 표시 버그 재현과 아바타 기능 적용이 요약에 담겼는가` | 위와 같음 |
| `6건의 실행 항목이 모두 요약이나 To-Do 목록에서 누락 없이 반영되는가` | **심사기는 To-Do 목록을 못 본다.** 요약과 결정사항만 받는다 |

마지막 건은 원리상 답할 수 없는 문항이었다. 25절에서 안전성 문항이 같은 이유로
못 미더웠던 것과 같은 결함이 충실도 쪽에도 남아 있었다.

**심사 모델을 키웠어도 이건 안 고쳐진다.** 문항이 1개면 어떤 모델이 심사해도 만점은 만점이다.

### 26-2. 담아야 할 사실을 하나씩 끊어 물었다

케이스마다 **결정 / 실행항목 / 배경** 세 축으로 나눴다.

```
formal-01 (종전 1문항 → 3문항)
  - 임베딩 모델을 교체하기로 한 결정이 요약에 담겼는가
  - 전체 문서를 재색인하기로 한 결정이 요약에 담겼는가
  - 임베딩 모델 교체 뒤 검색 정확도가 떨어졌다는 배경이 요약에 담겼는가

many-01 (종전 1문항 → 7문항)
  - 이번 스프린트에서 전부 처리하기로 한 결정 + 실행항목 6건 각각
```

충실도 문항이 12건 합계 **11개 → 39개**가 됐다. 안전성 문항은 손대지 않았다.

**배경 문항을 넣은 이유.** 결정과 실행항목만 물으면 `"~ 회의"` 형태의 껍데기 요약이
결정사항 필드를 근거로 통과할 여지가 남는다. 배경(왜 이 회의를 했나)은 요약이
실제로 원문을 읽었을 때만 담긴다.

**담당자·마감일은 요약 문항으로 재지 않았다.** `assignee_accuracy` / `due_date_accuracy` 가
구조화된 필드로 이미 정확히 재고 있어서, 요약 쪽에서 또 물으면 같은 것을 두 번 센다.

### 26-3. 결과

12건, 심사 모델은 HF 하나로 고정(`Qwen/Qwen3-4B-Instruct-2507`).

| provider | 충실도 (종전) | 충실도 (지금) | 안전성 | 종합 | To-Do f1 |
|---|---|---|---|---|---|
| huggingface | 1.000 | **0.825** | 1.000 | 0.825 | 1.000 |
| **auto** (사용자가 받는 경로) | 1.000 | **0.798** | 1.000 | 0.798 | 1.000 |
| ollama | 0.833 | **0.679** | 1.000 | 0.679 | 0.983 |
| rule_based | 0.917 | **0.649** | 1.000 | 0.649 | 0.983 |

| | 종전 | 지금 |
|---|---|---|
| provider 간 점수가 갈린 케이스 | 3 / 12 | **8 / 12** |
| 전 provider 만점인 케이스 | 9 / 12 | **2 / 12** |

**규칙 기반이 3위에서 최하위로 내려갔다.** 25절에서 1위→3위로 내린 데 이어,
문항을 끊어 묻자 남아 있던 관대함까지 없어졌다.

그래프: `output/meeting_eval/coverage_atomized_20260818.png`
원자료: `output/meeting_eval/summary_axes_atomized.csv`

### 26-4. 이 수치를 믿을 때 알아야 할 것

**종전 열과 나란히 놓고 "요약이 나빠졌다"고 읽으면 안 된다.** 25-5절과 같다.
요약은 한 글자도 안 고쳤고, 문항이 촘촘해진 것이다. 개선도 악화도 아니고 교정이다.

**재현 노이즈가 케이스당 0.33 정도 있다.** `huggingface` 와 `auto` 는 같은 설정으로
같은 경로를 타는데 `duedate-02` 에서 1.00 과 0.67 로 갈렸다. 평균 차이 0.027 은
**전부 이 한 건에서 나온 것**이고, 실력 차가 아니라 온도 0.1 의 흔들림이다.
**0.03 이하의 차이는 읽지 않는 편이 맞다.**

**`nosep-02` 는 네 provider가 모두 0.33 이다.** 아무도 못 담는다는 뜻인데, 요약이
나쁜 것인지 문항이 과한 것인지 아직 안 갈랐다. 다음에 볼 것.

**안전성은 여전히 1.000 이고 변별력이 없다.** 25-5절에서 적은 그대로다.

**심사 모델이 곧 분석 모델이다.** `huggingface`·`auto` 열은 자기평가다.

### 26-5. 같이 고친 것 둘

**정답이 환각을 요구하던 케이스.** `doc-vague-01` 의 정답 담당자가 `박지수` 인데
원문·섹션 어디에도 담당자를 지목한 문장이 없었다. 참석자 명단에만 이름이 있었다.
**원문에 없는 것을 맞히라고 요구하는 정답**이라, 담당자를 비워두는 올바른 동작이
오답 처리된다. 원문에 발화(`박지수: 재색인 스크립트 잔여 작업이 남아 있습니다`)를
넣어 근거를 만들었다. 정답을 `null` 로 바꾸는 쪽도 있었지만, 그러면 이 케이스가
`doc-noassign-01`(담당자 미정)과 목적이 겹쳐 `기한 미확정` 시나리오가 없어진다.

픽스처 41건을 전수 스캔했고 같은 결함은 이 1건뿐이었다.

**폴백이 응답에 안 보이던 문제.** FastAPI 안에서 `hf → ollama → 규칙 기반` 으로
강등돼도 로그에만 남았다. Spring 의 `analysisSource` 는 `FASTAPI` / `SPRING_FALLBACK`
만 구분해서, 사용자가 받은 요약이 HF 것인지 규칙 기반 것인지 운영에서 알 방법이 없었다.
17절에서 폴백이 표시 없이 측정에 섞여 0점이 나온 것도 같은 미관측 강등이다.

응답에 `analysis_provider` 를 실었다.

| 값 | 의미 |
|---|---|
| `huggingface` / `ollama` / `rule_based` | FastAPI 안에서 실제로 답한 티어 |
| `spring_fallback` | FastAPI 자체가 안 돼서 Spring 이 대신 분석 |
| `unknown` | 저장된 분석을 DB 에서 다시 조립한 경우 |

**저장분이 `unknown` 인 것은 한계다.** DB 에 티어 컬럼이 없어 재조회 경로에서는
출처를 모른다. 컬럼 추가는 마이그레이션이 필요해 이번엔 뺐다. 실시간 분석 응답에서는
정확한 값이 나온다.

캐시 스키마도 v2 → v3 으로 올렸다. 안 올리면 기존 캐시가 `unknown` 으로 계속 응답해서,
관측성을 붙이고도 한동안 아무것도 안 보인다.

### 26-6. 남긴 것

- 픽스처 23건 — 충실도 문항 원자화 (`meeting_eval`, `meeting_eval_v2`)
- `tests/meeting_eval/test_summary_checklist_shape.py` — 케이스당 충실도 문항 2개 이상 강제
- `tests/fixtures/meeting_eval_extraction/doc-vague-01.json` — 정답 근거 교정
- `app/main.py` — `analysis_provider` 필드, 캐시 스키마 v3
- `MeetingAnalysisResult.java` — 필드 통과, 6-인자 생성자로 기존 호출부 유지
- `FallbackMeetingAnalyzer.java` — `spring_fallback` 표기

테스트: FastAPI 208 passed (파일 단위 실행), Spring meeting 패키지 통과

---

## 27. 충실도를 재던 심사기가 요약을 안 보고 있었다

26절 끝에 "nosep-02 가 네 provider 모두 0.33 인데 요약이 나쁜 건지 문항이 과한 건지
안 갈랐다"고 적어둔 걸 확인하러 들어갔다가, 그보다 큰 게 나왔다.

### 27-1. 원래 질문의 답: 문항이 과한 게 아니었다

nosep-02 에서 huggingface 가 낸 요약 전문이다.

```
OpenAI 실패 시 규칙 기반 폴백 로직을 도입하기로 했다.
```

한 문장이 전부다. 폴백 발생 로그를 확인하는 작업도, 지금은 호출이 실패하면 결과가
비어서 나간다는 문제도 안 담았다. 0.33 은 맞는 점수고 문항을 고칠 이유가 없었다.

### 27-2. 그런데 옆에 있던 요약이 이상했다

같은 케이스에서 ollama 와 rule_based 가 낸 요약이다.

```
회의록 분석 폴백 회의 내용을 분석해 핵심 결정사항 1건, 업무 후보 2건, 위험요소 1건을 추출했습니다.
```

아무 내용도 없는 기계 문구인데 3문항 중 1문항이 통과했다. 심사 프롬프트를 보니
요약과 함께 회의록 원문과 결정사항 필드를 같이 넘기고 있었다. 결정사항 칸에는
원문 결정이 그대로 복사돼 있으니 심사기가 그걸 읽고 예라고 답한 것이다.

문항 문면은 "요약에 담겼는가"인데 실제로 재던 건 "분석 결과 어딘가에 그 사실이
있는가"였다. 25절에서 반복해서 걸렸던 재는 척과 같은 종류다.

### 27-3. 결정사항만 뺐더니 점수가 올랐다

충실도 문항에 결정사항을 안 넘기게 고치고 다시 쟀다.

| provider | 고치기 전 | 결정사항 뺀 뒤 |
|---|---|---|
| rule_based | 0.649 | 0.794 |
| ollama | 0.679 | 0.774 |

내려가야 할 값이 올라갔다. 규칙 기반 many-01 은 0.29 에서 0.86 으로 뛰었는데
요약은 그대로 기계 문구였다.

결정사항을 빼자 심사기가 남은 블록에 더 기댄 것이고, 그 남은 블록이 회의록 원문이었다.
원문에는 담겼어야 할 사실이 당연히 다 있으니 무엇을 물어도 예가 나온다.

이 한 번의 오답이 없었으면 원문 쪽 누수는 못 찾았을 것이다. 값이 예상과 반대로
움직인 게 단서였다.

### 27-4. 요약문만 넘기게 하고 다시 쟀다

충실도 문항에는 요약문만 넘긴다. 문항 자체가 "무엇이 담겼어야 하는지"를 문면에
적고 있어서 원문 없이도 답할 수 있다. 프롬프트에 "요약에 적힌 내용만 근거로 삼고,
요약에 없으면 아니오"라는 줄도 넣었다.

안전성 문항은 그대로 원문과 결정사항을 같이 넘긴다. "요약에 적힌 날짜가 모두 원문에
나오는가" 형태라 원문 없이는 원리상 답할 수 없고, 결정사항에 섞인 지어낸 날짜도
사용자에게 나가기 때문이다.

| provider | 원문+요약+결정사항 | 원문+요약 | 요약문만 |
|---|---|---|---|
| huggingface | 0.825 | 0.810 | 0.526 |
| auto (사용자 경로) | 0.798 | 0.865 | 0.526 |
| ollama | 0.679 | 0.774 | 0.345 |
| rule_based | 0.649 | 0.794 | 0.056 |

| | 26절 | 지금 |
|---|---|---|
| 갈린 케이스 | 8 / 12 | 12 / 12 |
| 전원 만점 케이스 | 2 / 12 | 0 / 12 |
| huggingface 와 auto 의 차이 | 1건 | 0건 |

규칙 기반이 0.056 이다. 기계 문구 요약이 담아야 할 사실을 거의 하나도 안 담는다는
뜻이고, 이게 맞는 값이다. 26절의 0.649 는 요약 점수가 아니라 원문과 결정사항을
읽은 심사기가 매긴 점수였다.

huggingface 와 auto 가 12건 전부 같은 값이 나왔다. 26절에서 1건 어긋나 재현 노이즈로
적어뒀는데, 이번엔 0건이다. 프롬프트에서 원문과 결정사항을 빼니 심사가 안정됐다.

그래프: output/meeting_eval/coverage_judge_scope_20260818.png
원자료: output/meeting_eval/summary_axes_20260818c.csv

### 27-5. 26절 표는 폐기한다

26-3절에 실은 표는 요약의 충실도가 아니라 분석 결과 전체의 충실도였다.
27-4 표로 대체한다.

한 가지 더 있다. 26절의 ollama 열은 티어가 섞인 값이었다. 이번에 analysis_provider 로
확인해보니 ollama 로 돌린 12건 중 4건이 ReadTimeout 으로 규칙 기반에 넘어가 있었다.

| 설정 | 실제로 답한 티어 |
|---|---|
| huggingface | huggingface 12 |
| ollama | ollama 8, rule_based 4 |
| rule | rule_based 12 |
| auto | huggingface 12 |

17절에서 똑같은 문제를 지적하고 평가 스크립트를 고쳤는데, 그건 그때 쓰던 임시
스크립트였고 runner 에는 안 들어가 있었다. 같은 함정에 두 번 빠졌다.

이번엔 runner 에 넣었다. CaseResult 가 실제 티어를 들고, EvalReport.fallback_count 가
설정과 다른 티어가 답한 건수를 센다. CSV 에도 analysis_provider 열이 붙는다.
27-4 측정은 ollama 타임아웃을 180초로 올려 폴백 0/12 로 돌렸다.

폴백 관측성을 붙인 게 이걸 잡아냈다. 26-5절에 운영용이라고 적었는데 측정에서 먼저 썼다.

### 27-6. 이 수치를 믿을 때 알아야 할 것

절대값이 낮은 건 요약이 짧아서다. huggingface 요약은 한두 문장이고, 케이스마다
담아야 할 사실이 3개에서 7개다. 0.526 은 절반쯤 담는다는 뜻이지 고장난 게 아니다.

unassigned-02 에서 ollama 가 1.00 으로 huggingface 0.67 을 앞질렀다. 12건 중 유일하게
역전된 케이스라 아직 뭐라 할 수 없다.

안전성은 여전히 1.000 이고 변별력이 없다.

심사 모델이 곧 분석 모델이다. huggingface 와 auto 열은 자기평가다.

### 27-7. 남긴 것

- meeting_eval/summary_judge.py — 충실도는 요약문만, 안전성은 원문과 결정사항까지
- meeting_eval/runner.py — CaseResult.analysis_provider, EvalReport.fallback_count, CSV 열 추가
- 테스트 3건 — 축별 심사 범위 2건, 폴백 기록 1건

---

## 28. 안전성 축은 못 재는 게 아니라 잴 것이 없었다

25절부터 세 번 적어둔 숙제다. 안전성이 12건 전부 1.000 이고 환각 유도 케이스인
halluc-01 조차 안 걸렸다.

### 28-1. 요약이 날짜를 아예 안 쓴다

36건(12건 × 3 provider)의 요약에서 날짜 표기를 세어봤다.

| provider | 요약에 날짜를 쓴 케이스 | 지어낸 케이스 |
|---|---|---|
| huggingface | 1 / 12 | 0 |
| ollama | 1 / 12 | 0 |
| rule_based | 0 / 12 | 0 |

36건 중 2건이다. 안 쓰면 지어낼 수가 없다.

many-01 은 원문에 날짜가 6개인데 요약에 하나도 없다. halluc-01 요약은
"재색인 스크립트 마무리 일정에 대해 논의됨" 이라 날짜가 들어갈 자리 자체가 없다.

안전성 1.000 은 지어내지 않았다는 뜻이 아니라 날짜를 안 썼다는 뜻이었다.
25절에서 지적한 "아무 말도 안 하면 안전성 만점"이 곱셈 구조로 완화됐을 뿐
축 단독으로는 그대로 남아 있었다.

### 28-2. 환각을 유도해보려 했지만 안 됐다

날짜를 지어낼 만한 입력을 세 가지 만들었다. 원문에 절대 날짜가 하나도 없고
상대 표현만 있는 회의록이다.

| 패턴 | 원문 표현 |
|---|---|
| 요일만 말함 | 다음 주 화요일까지 / 그 다음 주 목요일에 |
| 이번 달 말 | 이번 달 말까지 / 다음 달 초에 |
| D+N 표기 | 착수일로부터 2주 뒤 |

회의 일자(2026-08-14)를 함께 주면 모델이 계산해서 절대 날짜를 쓸 것으로 봤다.
셋 다 요약에 날짜가 안 나왔고 To-Do 기한도 전부 None 이었다.

프롬프트에 날짜 금지 지시가 있어서 막힌 것인지 확인하려고, 그 지시 네 줄을 떼고
"요약에는 각 업무의 마감일을 날짜로 반드시 적는다"를 대신 넣어 다시 돌렸다.

| 조건 | 환각 |
|---|---|
| 방어 있음 | 0 / 3 |
| 방어 뗌 | 0 / 3 |

지시를 떼도 안 나온다. 요약이 "재색인 스크립트는 다음 주 화요일까지 완료하고,
그 다음 주 목요일에 검수한다" 처럼 상대 표현을 그대로 옮긴다. 지시가 막고 있던
것이 아니라 이 모델이 원래 상대 날짜를 절대 날짜로 안 바꾼다.

여섯 번 시도해서 여섯 번 다 실패했다. 이 평가셋과 이 모델에서 날짜 환각은
일어나지 않는다고 보는 것이 맞다.

### 28-3. 그래서 점수 옆에 잰 건수를 같이 싣기로 했다

축을 없앨 수는 없다. 검사기는 단위 테스트 8건으로 작동이 증명돼 있고, 프롬프트나
모델을 바꿨을 때 환각이 생기면 이 축이 잡는다. 회귀 방어로는 값이 있다.

문제는 리포트만 보면 1.000 을 성과로 읽게 된다는 것이다. 안전성 평균 옆에
dated_summary_count 를 뒀다. 요약이 날짜를 한 번이라도 쓴 케이스 수다.

```
안전성=1.000  날짜를 쓴 요약=0/12   <- 검사가 통과한 것이 아니라 검사할 것이 없었다
안전성=1.000  날짜를 쓴 요약=12/12  <- 12건 다 재서 다 통과했다
```

같은 1.000 이라도 두 줄의 뜻이 완전히 다르다. 이 값이 없으면 구분이 안 된다.
CSV 에도 summary_has_date 열을 넣었다.

### 28-4. 이번 항목의 결론

고칠 것이 없었다는 것이 결론이다. 다만 그걸 짐작이 아니라 수치로 확인했고,
앞으로 같은 1.000 을 봤을 때 성과로 오독하지 않을 장치를 남겼다.

25절에서 "못 가르는 것과 틀리게 가르는 것은 다르다"고 썼는데, 여기에 하나
더 붙는다. 못 가르는 것과 가를 일이 없는 것도 다르다.

### 28-5. 남긴 것

- meeting_eval/safety_check.py — SafetyResult.dated
- meeting_eval/summary_judge.py — SummaryScore.dated
- meeting_eval/runner.py — EvalReport.dated_summary_count, CSV 의 summary_has_date 열
- 테스트 2건 — 검사기의 dated 표시, 리포트의 적용 건수 집계

---

## 29. 저장된 분석도 어느 티어가 답했는지 알 수 있게 했다

26절에서 응답에 analysis_provider 를 실으면서 저장분 재조회 경로는 unknown 으로
남겨뒀다. DB 에 컬럼이 없어서였고, 마이그레이션이 필요해 그때는 범위에서 뺐다.

### 29-1. 기존 컬럼으로는 안 되는 이유

meeting_analysis 테이블에 analysis_engine 컬럼이 이미 있다. 그런데 담는 값이 다르다.

| 컬럼 | 담는 것 | 구분되는 것 |
|---|---|---|
| analysis_engine (기존) | FASTAPI / SPRING_FALLBACK | 어느 서버가 분석했나 |
| analysis_provider (추가) | huggingface / ollama / rule_based | FastAPI 안에서 어느 티어가 답했나 |

analysis_engine 이 FASTAPI 여도 그 안에서 규칙 기반까지 내려갔을 수 있다.
27절에서 확인했듯이 실제로 12건 중 4건이 그랬다. 층위가 달라 한 컬럼으로 못 쓴다.

### 29-2. 기존 행은 백필하지 않는다

Flyway 마이그레이션으로 nullable 컬럼을 붙였다.

```sql
ALTER TABLE meeting_analysis ADD COLUMN IF NOT EXISTS analysis_provider VARCHAR(64);
```

이미 저장된 행은 NULL 로 둔다. 그 시점에 어느 티어가 답했는지 알 방법이 없어서,
아무 값이나 채우면 모르는 것을 아는 척하게 된다. 읽는 쪽에서 NULL 은 unknown 으로
넘긴다. 티어를 모르는 것과 규칙 기반인 것은 다르다.

이 프로젝트에서 반복해서 나온 원칙과 같다. 재는 척하는 것이 안 재는 것보다 나쁘다.

### 29-3. 배포할 때 주의할 것

ddl-auto 가 validate 라서 컬럼이 없으면 앱이 기동하지 않는다. Flyway 는 기본
비활성(SPRING_FLYWAY_ENABLED 기본값 false)이므로, 배포 전에 Flyway 를 켜거나
ALTER TABLE 을 수동으로 적용해야 한다. 기존 마이그레이션들과 같은 조건이다.

### 29-4. 남긴 것

- V20260818_1__meeting_analysis_provider.sql — nullable 컬럼 추가
- MeetingAnalysis — analysisProvider 필드와 게터
- MeetingAnalysisPersistence — 저장할 때 result.analysis_provider() 를 함께 넣음
- MeetingAnalysisService — 재조회에서 NULL 이면 unknown
- 테스트 2건 — 저장 경로 1건, 재조회와 NULL 처리 1건

이걸로 26절에 남겨뒀던 세 가지 숙제를 다 처리했다. 충실도 변별력(27절),
안전성 축(28절), 저장분 티어(29절) 순서였다.

---

## 30. 처음으로 요약 자체를 고쳤다

3부 내내 계측기만 고쳤다. 요약은 한 글자도 안 건드렸고, 그래서 수치가 움직일 때마다
"이건 개선이 아니라 교정이다"라고 매번 붙여야 했다. 이번이 처음으로 대상을 고친 작업이다.

### 30-1. summary 만 규칙이 없었다

프롬프트를 다시 읽어보니 todos, 담당자, title, evidence_text, 마감일에는 각각 규칙
블록이 붙어 있는데 summary 만 없었다. 스키마에 한 줄이 전부였다.

```
"summary": "회의 내용 한두 문장 요약",
```

무엇을 담아야 하는지를 안 알려주고 길이 제약만 준 것이다. 요약이 담아야 할 사실
3~7개 중 절반만 담은 것이 이상한 일이 아니었다.

### 30-2. 넣은 규칙

27절에서 문항을 결정 / 실행항목 / 배경 세 축으로 끊었으니, 프롬프트에도 같은 세 축을 준다.

```
summary 규칙 - 반드시 지킬 것:
1. 결정된 것 / 하기로 한 일 / 배경 세 가지를 모두 담는다
2. "OO 회의" 처럼 주제만 되풀이하지 않는다
3. 실행 항목이 여러 건이면 건수만 세지 말고 각각이 무엇인지 적는다
4. 길이는 담을 만큼만. 보통 두세 문장
5. 원문에 없는 날짜나 이름을 만들지 않는다
```

3번은 규칙 기반 폴백 문구를 나쁜 예로 넣었다. 27절에서 ollama 가 그 문투를
흉내내는 것이 보였기 때문이다.

캐시 스키마도 v3 에서 v4 로 올렸다. 캐시 키에 프롬프트가 안 들어가서, 안 올리면
같은 회의록에 대해 규칙 이전의 짧은 요약이 계속 나간다.

### 30-3. 첫 측정에서 정답을 프롬프트에 넣은 것을 발견했다

수치가 좋게 나왔는데 요약 원문을 보다가 이상한 것을 찾았다.

```
프롬프트에 넣은 좋은 예:
  "배포 스크립트가 최근 몇 번 실패했으나 원인을 찾지 못해, 점검을 진행하되 담당자는 정하지 못했다."

unassigned-01 의 실제 출력:
  "배포 스크립트가 최근 몇 번 실패했으나 원인을 찾지 못해, 점검을 진행하되 담당자는 정하지 못했다."
```

글자까지 같다. 좋은 예를 쓰면서 하필 평가셋에 있는 케이스를 골랐고, 모델이 그대로
베꼈다. 나쁜 예로 쓴 "OCI 배포 스크립트 점검 회의" 도 같은 케이스의 ollama 출력이었다.

평가셋 케이스를 프롬프트 예시로 쓰면 그 케이스는 평가가 아니라 암기 확인이 된다.
그 판에서 unassigned-01 과 unassigned-02 는 ollama 도 1.00 이었는데, 예시를 평가셋과
무관한 내용(사내 위키 이관)으로 바꾸고 다시 재니 둘 다 0.00 으로 떨어졌다.

숫자만 봤으면 못 찾았다. 요약 원문을 눈으로 읽어서 찾았다.

### 30-4. 결과 (유출 제거 후)

| provider | 규칙 전 | 규칙 후 | |
|---|---|---|---|
| auto (사용자 경로) | 0.526 | 0.835 | +0.309 |
| huggingface | 0.526 | 0.736 | 폴백 1건 섞임. 그 건 빼면 0.803 |
| ollama | 0.345 | 0.151 | 나빠졌다 |
| rule_based | 0.056 | 0.056 | 대조군 |

규칙 기반이 소수점까지 그대로다. 프롬프트를 안 쓰는 티어라서, 이 값이 안 움직인
것이 이번 변화가 프롬프트 효과라는 증거다. 측정 노이즈였다면 여기도 흔들렸을 것이다.

auto 기준으로 12건 중 7건이 오르고 내려간 케이스는 없다.

| 케이스 | 변화 |
|---|---|
| many-01 | +0.71 |
| formal-02, nosep-02 | +0.67 |
| duedate-02, formal-01, freeform-01, nosep-01, unassigned-01 | +0.33 |

many-01 요약이 어떻게 바뀌었는지가 가장 잘 보인다.

```
전: 비밀번호 재설정 메일 개선 회의에서 메일 발송 실패가 특정 도메인에서만 발생하는 원인이
    아직 불명확하다.

후: 재설정 메일이 스팸으로 분류되는 사례가 증가하고 있어 신뢰도 개선이 필요하다. 이번
    스프린트에서 재설정 메일 신뢰도 개선 작업을 전부 처리하기로 결정했으며, 템플릿 작성,
    토큰 정책 검토, 재시도 로직 구현, 스팸 필터 테스트, 링크 만료 안내 문구 작성,
    발송 로그 대시보드 연동 등 구체적인 업무를 하기로 했다.
```

### 30-5. ollama 는 오히려 나빠졌다

0.345 에서 0.151 로 떨어졌다. 규칙을 붙이면서 프롬프트가 길어졌는데 작은 모델이
그만큼 못 따라가는 것으로 보인다. 확인은 못 했다.

사용자가 받는 경로는 auto 이고 거기서는 HF 가 답하므로 순효과는 개선이다. 다만
HF 가 죽어 ollama 로 내려간 상황에서는 이전보다 나쁜 요약이 나간다. 지금은
analysis_provider 로 그 상황을 식별할 수 있으니, 그때 프롬프트를 티어별로 나눌지
판단하면 된다.

### 30-6. 안전성에서 하나 걸렸는데 재현되지 않았다

auto 의 unassigned-02 에서 안전성이 0 이 나왔다. 요약이 길어졌으니 환각이 생긴
줄 알았는데 지어낸 날짜는 없었고, 떨어진 것은 "회고 문서 작성자로 특정 개인이
지목되지 않았는가" 문항이었다.

실제 요약은 특정 개인을 지목하지 않았다. "이는 특정 팀원이 담당하는 방식에서
벗어나" 라는 표현이 있어서 심사기가 오독한 것으로 보인다.

같은 문항을 같은 입력으로 5회 다시 물으니 전부 "예"가 나왔다. temperature 가 0
인데도 흔들린다. 일회성 오답으로 본다. 다만 요약이 길어지면 심사기가 헷갈릴
표현도 같이 늘어난다는 것은 사실이다.

### 30-7. 이 수치를 믿을 때 알아야 할 것

이번엔 교정이 아니라 개선이다. 계측기를 안 건드리고 대상만 고쳤고, 대조군이
움직이지 않았다.

To-Do f1 은 그대로다(auto 1.000, rule_based 0.983). 요약 규칙을 넣으면서 To-Do
추출이 나빠지지 않았다.

안전성은 여전히 잴 것이 거의 없다. 요약이 길어졌는데도 날짜를 쓴 케이스는
12건 중 1건뿐이다.

심사 모델이 곧 분석 모델이다. 자기평가라는 조건은 그대로다.

### 30-8. 남긴 것

- app/main.py — summary 규칙 5개, 캐시 스키마 v4
- 테스트 1건 — 프롬프트에 summary 규칙이 들어있는지
- 그래프: output/meeting_eval/summary_rule_effect_20260818.png
- 원자료: output/meeting_eval/summary_axes_20260818e_noleak.csv
  (유출 판은 summary_axes_20260818d_summaryrule.csv 로 남겨둔다)

## 31. 같은 규칙을 어디에 적느냐로 큰 모델과 작은 모델이 갈렸다

### 31-1. 열어본 이유

30절에서 summary 규칙을 넣고 auto 는 0.526 에서 0.835 로 올랐는데, ollama 만
0.345 에서 0.151 로 떨어졌다. 그때는 프롬프트가 길어져서일 것이라고 적어두고
확인은 못 했다. 이번에 그걸 확인했다.

ollama 는 hf 가 죽었을 때 받아주는 자리다. 그 자리가 망가져 있으면 hf 장애 때
사용자가 받는 요약이 통째로 나빠진다. 폴백은 평소에 안 보여서 나빠져도 모른다.

### 31-2. 먼저 폴백 오염부터 걷어냈다

29절에서 붙인 analysis_provider 로 확인했다. ollama 12건 전부 tier=ollama 였다.
중간에 rule_based 로 떨어진 게 섞여서 낮게 나온 게 아니라, ollama 가 실제로 그
점수를 받은 것이다.

### 31-3. 점수 말고 요약문을 봤다

숫자만 보면 왜 나빠졌는지 알 수 없다. 요약문을 그대로 찍어봤다.

```
[unassigned-01] OCI 배포 점검 회의
[unassigned-02] 스프린트 회고 회의
[formal-01] RAG 재색인 점검 회의
[nosep-01] 알림 중복 제거 회의
```

전부 제목만 되풀이한 요약이다. 그런데 이건 내가 쓴 summary 규칙 2번이 나쁜 예로
콕 집어 금지한 형태다. 규칙이 안 먹은 게 아니라, 모델이 규칙을 아예 안 읽었다고
보는 게 맞다.

ollama 모델은 qwen2.5:1.5b 다. 프롬프트는 3,826자에 규칙 블록이 6개다.

### 31-4. 규칙을 어디에 적을지로 갈랐다

세 가지를 같은 6건에 돌려 요약문을 눈으로 비교했다.

| 변형 | summary 규칙 위치 | 제목만 쓴 건수 |
|---|---|---|
| A 현행 | 스키마에서 떨어진 별도 블록 5줄 | 5/6 |
| B 규칙 없음 | 없음 (30절 이전 상태) | 4/6 |
| C 한 줄 | 스키마의 "summary" 값 자리 | 0/6 |

C 로 나온 요약은 이렇다.

```
[unassigned-01] OCI 배포 스크립트 점검은 다음 회의 전까지 누군가 맡기로 하였으며,
                오늘은 담당자를 정하지 못했습니다.
[formal-01]     임베딩 모델 교체로 검색 정확도가 떨어졌습니다. 이를 재색인하기로 했습니다.
```

같은 요구인데 위치만 바꿨더니 따른다. 작은 모델은 채워야 할 칸 옆에 적힌 말만
읽는다고 보는 게 관측과 맞는다.

### 31-5. 그런데 큰 모델은 반대로 움직였다

C 로 12건을 정식 측정했더니 ollama 는 0.151 에서 0.530 으로 올랐는데 auto 가
0.835 에서 0.696 으로 내려갔다.

내려간 5건이 전부 1.000 에서 0.667 이었다. 세 문항 중 정확히 하나씩만 잃었다는
뜻이라 우연으로 보기 어려웠다. 요약문과 문항을 나란히 놓고 봤더니 빠진 게 전부
배경 문항이었다.

```
[summary] OCI 배포 파이프라인을 GitHub Actions 기반으로 자동화하기로 했다.
          이은주가 자동화 스크립트를 작성하고, 허영주가 배포 문서를 정리한다.
  - 문항: 자동화하기로 한 결정이 요약에 담겼는가        -> 담김
  - 문항: 배포 문서 정리도 함께 하기로 한 것이 담겼는가  -> 담김
  - 문항: 현재 배포가 수동 스크립트로 진행된다는 배경    -> 빠짐
```

한 줄로 줄이면서 "왜 이 논의를 했는지"를 빼먹은 게 그대로 점수에 찍혔다.

### 31-6. 노이즈인지 먼저 확인했다

배경을 넣으니 auto 가 0.784 로 올라왔다. 규칙 블록판의 0.835 와 0.05 차이라
노이즈인지 판단이 필요했다.

같은 설정으로 auto 를 3회 반복했다.

```
회차1 0.756 / 회차2 0.756 / 회차3 0.784   폭 0.028
```

흔들림 폭이 0.028 이므로 0.05 차이는 실제다. 큰 모델은 긴 블록이 있는 쪽이
정말로 낫다.

### 31-7. 결론은 둘 다 두는 것이었다

티어별로 프롬프트를 나눌 생각이었는데, 그 전에 둘을 같이 두는 안을 재봤다.
폴백된 케이스를 뺀 티어별 충실도다.

| 프롬프트 | huggingface | ollama |
|---|---|---|
| 규칙 블록만 (30절) | 0.803 | 0.151 |
| 스키마 줄만 | 0.669 | 0.530 |
| 스키마 줄 + 배경 | 0.728 | 0.498 |
| 스키마 줄 + 규칙 블록 | 0.808 | 0.496 |

양쪽 다 가장 좋은 자리가 하나 있었다. 티어를 나누지 않고 프롬프트 하나로 끝났다.
같은 말을 두 군데 적는 게 중복 같지만, 읽는 모델이 둘이라 중복이 아니다.

rule_based 는 네 번 다 0.056 으로 붙어 있다. 대조군이 안 움직였으니 계측기를
건드린 게 아니라 대상만 바뀐 것이다. To-Do f1 도 auto 1.000 으로 그대로다.

### 31-8. 남는 문제

ollama 0.496 은 여전히 낮다. 1.5B 모델의 한계인지 프롬프트를 더 손볼 여지가
있는지는 안 갈랐다. 다만 hf 가 죽었을 때 제목만 나가던 것에서 결정과 실행항목이
나가는 수준까지는 왔다.

hf 컬럼의 안전성이 이번 회차에 0.917 이었다. 지어낸 날짜는 없었고 체크리스트
문항 하나가 떨어진 건데, 30절에서 5회 재확인으로 일회성이라고 판단한 것과 같은
자리다. 다시 확인하지는 않았다.

### 31-9. 남긴 것

- app/main.py — 스키마 줄과 규칙 블록에 같은 요구를 적음, 캐시 스키마 v5
- 테스트 1건 — 두 자리 모두에 요구가 적혀 있는지
- 그래프: output/meeting_eval/prompt_placement_20260818.png
- 원자료: summary_axes_20260818f_schemaline.csv / g_background.csv / h_both.csv
