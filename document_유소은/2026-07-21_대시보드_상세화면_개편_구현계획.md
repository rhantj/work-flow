# 대시보드 상세화면 개편 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 대시보드 상세 4개 화면(진행률 분석/블로커 관리/진행 중 업무/전체 진행률)의 카드·태그·버튼을
시안(`dashboard_edited_v1.png`)에 맞춰 교체하고, AI 버튼 2개는 기존 RAG 챗봇 패널을 재사용하도록 연결한다.

**Architecture:** 지연위험도 ML 파이프라인(HuggingFace `delay_model.pkl` → LightGBM 추론 → `ml_predictions`)은
이미 완성돼 있으므로 변경하지 않는다. 백엔드는 `DashboardTaskDto`/`ProgressDetailResponse`에 날짜 필드
2~3개만 추가하고, 프론트엔드는 그 값을 기존 훅(`useDashboardTasks`/`useDashboardProgress`)으로 그대로
받아 4개 상세 페이지 컴포넌트에서 카드/태그/버튼을 다시 그린다. AI 버튼 2개는 새 백엔드를 만들지 않고
`window.dispatchEvent` 기반 커스텀 이벤트로 기존 `AIAssistant` 패널을 열고 질문을 자동 전송한다.

**Tech Stack:** Spring Boot(Java, Gradle, JUnit5+Mockito+AssertJ) / React 19 + TypeScript + Vite + Vitest + Testing Library / 기존 FastAPI RAG·ML 파이프라인(무변경).

## Global Constraints

- 지연위험도 모델·추론 파이프라인·HuggingFace 연동 설정은 이번 작업에서 건드리지 않는다 (이미 동작).
- 새 백엔드 엔드포인트를 추가하지 않는다. AI 버튼 2개는 기존 RAG 질의(`useRagQuery`)를 재사용한다.
- 대시보드 상세 페이지 컴포넌트(ProgressPage/BlockersPage/InProgressPage/DashProgressPage)는 현재
  테스트가 전혀 없는 영역이다 — 기존 관례를 따라 이 4개 파일 자체에는 새 RTL 테스트를 추가하지 않고,
  대신 (a) 순수 로직(유틸 함수)과 (b) 공용 AI 이벤트 배선에 대해서만 자동화 테스트를 작성한 뒤,
  마지막 Task에서 개발 서버로 4개 화면을 직접 확인한다.
- 색상: 평균 지연 `#F97316`, 지연 위험 태그 `orange-100/orange-700`, 업데이트 필요 `#F59E0B`,
  지연 위험(InProgress) `#EF4444`, D-Day `#7048E8`, 계획 대비 뒤처짐 `#EF4444`/앞섬 `#10B981`.

---

## Task 1: 백엔드 — `DashboardTaskDto`에 생성/수정 시각 추가

**Files:**
- Modify: `App/backend_spring/src/main/java/dashboard/DTO/DashboardTaskDto.java`
- Modify: `App/backend_spring/src/main/java/dashboard/service/DashboardService.java:325-339` (`toDashboardTaskDto`)
- Test: `App/backend_spring/src/test/java/dashboard/service/DashboardServiceTest.java`

**Interfaces:**
- Produces: `DashboardTaskDto.createdAt(): String`, `DashboardTaskDto.updatedAt(): String` — ISO-8601
  문자열(`DateTimeFormatter.ISO_LOCAL_DATE_TIME`), null 가능. Task 3(프론트 타입)에서 그대로 소비.

- [ ] **Step 1: 실패하는 테스트 작성**

`App/backend_spring/src/test/java/dashboard/service/DashboardServiceTest.java` 상단 import에
`import dashboard.DTO.DashboardTaskDto;` 를 추가하고, 파일 끝(마지막 `}` 앞)에 아래 테스트를 추가한다.

```java
    @Test
    void getTasksIncludesCreatedAtAndUpdatedAt() {
        when(demoDataService.resolveProjectId("demo-project")).thenReturn(1L);
        Task task = taskWithId(10L, 5L);
        ReflectionTestUtils.setField(task, "createdAt", LocalDateTime.of(2026, 7, 1, 9, 0));
        ReflectionTestUtils.setField(task, "updatedAt", LocalDateTime.of(2026, 7, 19, 15, 30));
        when(taskRepository.findByProjectIdOrderByStatusAscPositionAsc(1L)).thenReturn(List.of(task));

        List<DashboardTaskDto> result = newService().getTasks("demo-project");

        assertThat(result).hasSize(1);
        assertThat(result.get(0).createdAt()).isEqualTo("2026-07-01T09:00:00");
        assertThat(result.get(0).updatedAt()).isEqualTo("2026-07-19T15:30:00");
    }
```

- [ ] **Step 2: 테스트 실패 확인**

Run (in `App/backend_spring`): `./gradlew test --tests "dashboard.service.DashboardServiceTest.getTasksIncludesCreatedAtAndUpdatedAt"`
Expected: FAIL — `DashboardTaskDto`에 `createdAt()`/`updatedAt()` 메서드가 없어 컴파일 에러.

- [ ] **Step 3: `DashboardTaskDto`에 필드 추가**

`App/backend_spring/src/main/java/dashboard/DTO/DashboardTaskDto.java` 전체를 아래로 교체:

```java
package dashboard.DTO;

import io.swagger.v3.oas.annotations.media.Schema;

@Schema(description = "대시보드 업무 목록 항목")
public record DashboardTaskDto(
    @Schema(description = "업무 ID", example = "42") String id,
    @Schema(description = "업무 제목", example = "발표자료 초안 작성") String title,
    @Schema(description = "카테고리", example = "presentation") String category,
    @Schema(description = "상태", example = "inprogress") String status,
    @Schema(description = "담당자 ID", example = "1") String assigneeId,
    @Schema(description = "담당자 이름", example = "김민준") String assigneeName,
    @Schema(description = "마감일(YYYY-MM-DD)", example = "2026-07-21") String dueDate,
    @Schema(description = "우선순위", example = "high") String priority,
    @Schema(description = "업무 설명") String description,
    @Schema(description = "업무 생성 출처", example = "MEETING_AI") String sourceType,
    @Schema(description = "같은 상태 안에서의 칸반 카드 순서", example = "0.0") double position,
    @Schema(description = "업무 생성 시각(ISO-8601)", example = "2026-07-01T09:00:00") String createdAt,
    @Schema(description = "업무 마지막 수정 시각(ISO-8601)", example = "2026-07-19T15:30:00") String updatedAt
) {
}
```

- [ ] **Step 4: `DashboardService.toDashboardTaskDto()` 매핑 갱신**

`App/backend_spring/src/main/java/dashboard/service/DashboardService.java:325-339`의
`toDashboardTaskDto` 메서드를 아래로 교체:

```java
    private DashboardTaskDto toDashboardTaskDto(Task task) {
        return new DashboardTaskDto(
            String.valueOf(task.getId()),
            task.getTitle(),
            task.getCategory(),
            task.getStatus(),
            task.getAssigneeId() == null ? null : String.valueOf(task.getAssigneeId()),
            resolveUserName(task.getAssigneeId()),
            task.getDueDate() == null ? null : task.getDueDate().toString(),
            task.getPriority(),
            task.getDescription(),
            task.getSourceType(),
            task.getPosition(),
            task.getCreatedAt() == null ? null : task.getCreatedAt().format(DateTimeFormatter.ISO_LOCAL_DATE_TIME),
            task.getUpdatedAt() == null ? null : task.getUpdatedAt().format(DateTimeFormatter.ISO_LOCAL_DATE_TIME)
        );
    }
```

(`DateTimeFormatter`는 이미 파일 상단에 import돼 있다 — 그대로 둔다.)

- [ ] **Step 5: 테스트 통과 확인**

Run: `./gradlew test --tests "dashboard.service.DashboardServiceTest"`
Expected: PASS (기존 테스트 3개 + 신규 1개 모두 통과)

- [ ] **Step 6: 커밋**

```bash
git add App/backend_spring/src/main/java/dashboard/DTO/DashboardTaskDto.java \
        App/backend_spring/src/main/java/dashboard/service/DashboardService.java \
        App/backend_spring/src/test/java/dashboard/service/DashboardServiceTest.java
git commit -m "feat(dashboard): DashboardTaskDto에 createdAt/updatedAt 추가"
```

---

## Task 2: 백엔드 — `ProgressDetailResponse`에 프로젝트 마감일/생성일 추가

**Files:**
- Modify: `App/backend_spring/src/main/java/dashboard/DTO/ProgressDetailResponse.java`
- Modify: `App/backend_spring/src/main/java/dashboard/service/DashboardService.java` (생성자, `getProgressDetail`)
- Modify: `App/backend_spring/src/test/java/dashboard/controller/DashboardControllerTest.java:67,99` (컴파일 유지)
- Test: `App/backend_spring/src/test/java/dashboard/service/DashboardServiceTest.java`

**Interfaces:**
- Consumes: `com.workflowai.project.Project`(기존, `getDeadline(): LocalDate`, `getCreatedAt(): LocalDateTime`),
  `com.workflowai.project.ProjectRepository`(기존 `JpaRepository<Project, Long>` — `findById(Long)` 상속 메서드 사용).
- Produces: `ProgressDetailResponse.projectDeadline(): String`, `ProgressDetailResponse.projectCreatedAt(): String`
  (둘 다 `YYYY-MM-DD`, null 가능) — Task 3(프론트 타입)을 거쳐 Task 8(InProgressPage D-Day)과
  Task 9(DashProgressPage 계획대비·마감 D-day)에서 사용.

- [ ] **Step 1: 실패하는 테스트 작성**

`DashboardServiceTest.java` 상단 import에 아래 3줄을 추가:
```java
import com.workflowai.project.Project;
import com.workflowai.project.ProjectRepository;
import dashboard.DTO.ProgressDetailResponse;
import java.util.Optional;
```
(`java.util.List`, `java.time.LocalDate`, `java.time.LocalDateTime`는 이미 import돼 있다.)

`@Mock` 필드 목록(`private FastApiDashboardClient fastApiDashboardClient;` 다음 줄)에 추가:
```java
    @Mock private ProjectRepository projectRepository;
```

`newService()` 헬퍼를 아래로 교체(마지막 인자 추가):
```java
    private DashboardService newService() {
        return new DashboardService(
            taskRepository, milestoneRepository, activityRepository, mlPredictionRepository,
            userRepository, projectMemberRepository, demoDataService, fastApiDashboardClient,
            projectRepository
        );
    }
```

파일 끝(마지막 `}` 앞)에 테스트 추가:
```java
    @Test
    void getProgressDetailIncludesProjectDeadlineAndCreatedAt() {
        when(demoDataService.resolveProjectId("demo-project")).thenReturn(1L);
        when(taskRepository.findByProjectIdOrderByCreatedAtDesc(1L)).thenReturn(List.of());
        when(milestoneRepository.findByProjectIdOrderByDueDateAsc(1L)).thenReturn(List.of());
        when(mlPredictionRepository.findByProjectIdAndTargetTypeAndModelTypeOrderByTargetIdAscCreatedAtDesc(
            eq(1L), eq("task"), eq("delay_risk")
        )).thenReturn(List.of());
        Project project = new Project("스마트 주차 관리 시스템", "team", LocalDate.of(2026, 8, 15), "설명");
        ReflectionTestUtils.setField(project, "createdAt", LocalDateTime.of(2026, 6, 1, 10, 0));
        when(projectRepository.findById(1L)).thenReturn(Optional.of(project));

        ProgressDetailResponse result = newService().getProgressDetail("demo-project");

        assertThat(result.projectDeadline()).isEqualTo("2026-08-15");
        assertThat(result.projectCreatedAt()).isEqualTo("2026-06-01");
    }

    @Test
    void getProgressDetailReturnsNullDatesWhenProjectMissing() {
        when(demoDataService.resolveProjectId("demo-project")).thenReturn(1L);
        when(taskRepository.findByProjectIdOrderByCreatedAtDesc(1L)).thenReturn(List.of());
        when(milestoneRepository.findByProjectIdOrderByDueDateAsc(1L)).thenReturn(List.of());
        when(mlPredictionRepository.findByProjectIdAndTargetTypeAndModelTypeOrderByTargetIdAscCreatedAtDesc(
            eq(1L), eq("task"), eq("delay_risk")
        )).thenReturn(List.of());
        when(projectRepository.findById(1L)).thenReturn(Optional.empty());

        ProgressDetailResponse result = newService().getProgressDetail("demo-project");

        assertThat(result.projectDeadline()).isNull();
        assertThat(result.projectCreatedAt()).isNull();
    }
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `./gradlew test --tests "dashboard.service.DashboardServiceTest"`
Expected: FAIL — 컴파일 에러(`DashboardService` 생성자 인자 수 불일치, `projectDeadline()` 없음).

- [ ] **Step 3: `ProgressDetailResponse`에 필드 추가**

`App/backend_spring/src/main/java/dashboard/DTO/ProgressDetailResponse.java` 전체 교체:

```java
package dashboard.DTO;

import io.swagger.v3.oas.annotations.media.Schema;
import java.util.List;

@Schema(description = "전체 진행률 상세")
public record ProgressDetailResponse(
    @Schema(description = "전체 업무 수", example = "14") long totalTasks,
    @Schema(description = "완료 업무 수", example = "4") long doneTasks,
    @Schema(description = "완료율 (%)", example = "29") long progressPercent,
    @Schema(description = "마일스톤별 진행 현황") List<MilestoneProgressDto> milestones,
    @Schema(description = "카테고리별 완료 현황") List<CategoryProgressDto> categoryBreakdown,
    @Schema(description = "AI 지연 위험도 (정상 제외, 주의/위험만)") List<DelayRiskDto> delayRisks,
    @Schema(description = "이 프로젝트에 대해 AI 지연 위험도 예측이 한 번이라도 실행됐는지 여부", example = "true")
    boolean hasPredictions,
    @Schema(description = "프로젝트 마감일 (YYYY-MM-DD)", example = "2026-08-15") String projectDeadline,
    @Schema(description = "프로젝트 생성일 (YYYY-MM-DD)", example = "2026-06-01") String projectCreatedAt
) {
}
```

- [ ] **Step 4: `DashboardService`에 `ProjectRepository` 주입**

`App/backend_spring/src/main/java/dashboard/service/DashboardService.java` 상단 import 블록에
(`import com.workflowai.project.ProjectMember;` 다음 줄) 추가:
```java
import com.workflowai.project.Project;
import com.workflowai.project.ProjectRepository;
```

필드 선언 블록(`private final FastApiDashboardClient fastApiDashboardClient;` 다음)에 추가:
```java
    private final ProjectRepository projectRepository;
```

생성자를 아래로 교체(마지막 인자 추가):
```java
    public DashboardService(
        TaskRepository taskRepository,
        MilestoneRepository milestoneRepository,
        ActivityRepository activityRepository,
        MlPredictionRepository mlPredictionRepository,
        UserRepository userRepository,
        ProjectMemberRepository projectMemberRepository,
        DemoDataService demoDataService,
        FastApiDashboardClient fastApiDashboardClient,
        ProjectRepository projectRepository
    ) {
        this.taskRepository = taskRepository;
        this.milestoneRepository = milestoneRepository;
        this.activityRepository = activityRepository;
        this.mlPredictionRepository = mlPredictionRepository;
        this.userRepository = userRepository;
        this.projectMemberRepository = projectMemberRepository;
        this.demoDataService = demoDataService;
        this.fastApiDashboardClient = fastApiDashboardClient;
        this.projectRepository = projectRepository;
    }
```

- [ ] **Step 5: `getProgressDetail()`에서 프로젝트 조회 후 응답에 포함**

같은 파일의 `getProgressDetail` 메서드(기존 124-148줄)를 아래로 교체:

```java
    public ProgressDetailResponse getProgressDetail(String projectIdParam) {
        Long projectId = demoDataService.resolveProjectId(projectIdParam);
        List<Task> tasks = taskRepository.findByProjectIdOrderByCreatedAtDesc(projectId);

        long total = tasks.size();
        long done = tasks.stream().filter(t -> STATUS_DONE.equals(t.getStatus())).count();
        long progressPercent = total == 0 ? 0 : Math.round(done * 100.0 / total);

        List<MilestoneProgressDto> milestones = buildMilestoneProgress(projectId, tasks);
        List<CategoryProgressDto> categoryBreakdown = buildCategoryBreakdown(tasks);

        List<MlPrediction> latestPredictions = latestPredictionsByTarget(projectId);
        boolean hasPredictions = !latestPredictions.isEmpty();

        Map<Long, Task> tasksById = new LinkedHashMap<>();
        tasks.forEach(t -> tasksById.put(t.getId(), t));

        List<DelayRiskDto> delayRisks = latestPredictions.stream()
            .filter(p -> !RISK_RESULT_NORMAL.equals(p.getResult()))
            .map(p -> toDelayRiskDto(p, tasksById.get(p.getTargetId())))
            .filter(java.util.Objects::nonNull)
            .toList();

        Project project = projectRepository.findById(projectId).orElse(null);
        String projectDeadline = project == null || project.getDeadline() == null
            ? null : project.getDeadline().toString();
        String projectCreatedAt = project == null || project.getCreatedAt() == null
            ? null : project.getCreatedAt().toLocalDate().toString();

        return new ProgressDetailResponse(
            total, done, progressPercent, milestones, categoryBreakdown, delayRisks, hasPredictions,
            projectDeadline, projectCreatedAt
        );
    }
```

- [ ] **Step 6: 컨트롤러 테스트의 기존 `ProgressDetailResponse` 생성 호출부 컴파일 맞추기**

`App/backend_spring/src/test/java/dashboard/controller/DashboardControllerTest.java:67`의

```java
        ProgressDetailResponse response = new ProgressDetailResponse(
            14, 4, 29, List.of(), List.of(), List.of(), false
        );
```
를 아래로 교체:
```java
        ProgressDetailResponse response = new ProgressDetailResponse(
            14, 4, 29, List.of(), List.of(), List.of(), false, null, null
        );
```

같은 파일 `:99`의

```java
        ProgressDetailResponse response = new ProgressDetailResponse(
            14, 4, 29, List.of(), List.of(), List.of(), true
        );
```
를 아래로 교체:
```java
        ProgressDetailResponse response = new ProgressDetailResponse(
            14, 4, 29, List.of(), List.of(), List.of(), true, null, null
        );
```

- [ ] **Step 7: 테스트 통과 확인**

Run: `./gradlew test --tests "dashboard.service.DashboardServiceTest" --tests "dashboard.controller.DashboardControllerTest"`
Expected: PASS (전부)

- [ ] **Step 8: 커밋**

```bash
git add App/backend_spring/src/main/java/dashboard/DTO/ProgressDetailResponse.java \
        App/backend_spring/src/main/java/dashboard/service/DashboardService.java \
        App/backend_spring/src/test/java/dashboard/service/DashboardServiceTest.java \
        App/backend_spring/src/test/java/dashboard/controller/DashboardControllerTest.java
git commit -m "feat(dashboard): ProgressDetailResponse에 projectDeadline/projectCreatedAt 추가"
```

---

## Task 3: 프론트엔드 — 타입 추가 + 날짜 유틸 헬퍼

**Files:**
- Modify: `App/frontend/src/dashboard/libs/types/dashboard.ts`
- Modify: `App/frontend/src/dashboard/libs/utils/dashboardTaskUtils.ts`
- Test: `App/frontend/src/dashboard/libs/utils/dashboardTaskUtils.test.ts` (신규)

**Interfaces:**
- Consumes: Task 1/2에서 추가한 백엔드 필드(camelCase 그대로 온다).
- Produces: `daysSince(dateStr): number | null` — Task 7(BlockersPage 체류일), Task 8(InProgressPage
  업데이트 필요/체류일)에서 사용. `formatRelativeDate(dateStr): string` — Task 8(업데이트 날짜 표시)에서 사용.
  `formatDashboardDueDate`(기존 시그니처 유지, 전체 ISO 일시 문자열도 받아들이도록 견고화)와
  `daysUntilDue`(기존, 무변경)는 Task 8/9에서 D-Day 계산에 계속 쓰인다.

- [ ] **Step 1: 실패하는 테스트 작성**

새 파일 `App/frontend/src/dashboard/libs/utils/dashboardTaskUtils.test.ts` 생성:

```ts
import { afterEach, beforeEach, describe, expect, it, vi } from "vitest";
import { daysSince, formatDashboardDueDate, formatRelativeDate } from "./dashboardTaskUtils";

describe("formatDashboardDueDate", () => {
  it("formats a date-only string as MM.DD", () => {
    expect(formatDashboardDueDate("2026-07-21")).toBe("07.21");
  });

  it("formats a full ISO datetime string as MM.DD", () => {
    expect(formatDashboardDueDate("2026-07-21T09:30:00")).toBe("07.21");
  });

  it("returns 미정 when null", () => {
    expect(formatDashboardDueDate(null)).toBe("미정");
  });
});

describe("daysSince / formatRelativeDate", () => {
  beforeEach(() => {
    vi.useFakeTimers();
    vi.setSystemTime(new Date("2026-07-21T12:00:00"));
  });

  afterEach(() => {
    vi.useRealTimers();
  });

  it("daysSince returns 0 for a timestamp earlier today", () => {
    expect(daysSince("2026-07-21T08:00:00")).toBe(0);
  });

  it("daysSince returns elapsed whole days for an earlier timestamp", () => {
    expect(daysSince("2026-07-18T12:00:00")).toBe(3);
  });

  it("daysSince returns null when given null", () => {
    expect(daysSince(null)).toBeNull();
  });

  it("formatRelativeDate returns 오늘 for today", () => {
    expect(formatRelativeDate("2026-07-21T08:00:00")).toBe("오늘");
  });

  it("formatRelativeDate returns 어제 for the previous calendar day", () => {
    expect(formatRelativeDate("2026-07-20T23:50:00")).toBe("어제");
  });

  it("formatRelativeDate returns n일 전 for 2~29 days ago", () => {
    expect(formatRelativeDate("2026-07-15T12:00:00")).toBe("6일 전");
  });

  it("formatRelativeDate falls back to MM.DD for 30+ days ago", () => {
    expect(formatRelativeDate("2026-06-01T12:00:00")).toBe("06.01");
  });

  it("formatRelativeDate returns 미정 when null", () => {
    expect(formatRelativeDate(null)).toBe("미정");
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run (in `App/frontend`): `pnpm exec vitest run src/dashboard/libs/utils/dashboardTaskUtils.test.ts`
Expected: FAIL — `daysSince`/`formatRelativeDate`가 존재하지 않아 임포트 에러, 일부 케이스 실패.

- [ ] **Step 3: 타입 추가**

`App/frontend/src/dashboard/libs/types/dashboard.ts`의 `DashboardTaskDto`를 아래로 교체:

```ts
export interface DashboardTaskDto {
  id: string;
  title: string;
  category: string | null;
  status: string;
  assigneeId: string | null;
  assigneeName: string | null;
  dueDate: string | null;
  priority: string | null;
  description: string | null;
  sourceType: string | null;
  position: number;
  createdAt: string | null;
  updatedAt: string | null;
}
```

`ProgressDetailResponse`를 아래로 교체:

```ts
export interface ProgressDetailResponse {
  totalTasks: number;
  doneTasks: number;
  progressPercent: number;
  milestones: MilestoneProgressDto[];
  categoryBreakdown: CategoryProgressDto[];
  delayRisks: DelayRiskDto[];
  hasPredictions: boolean;
  projectDeadline: string | null;
  projectCreatedAt: string | null;
}
```

- [ ] **Step 4: 유틸 함수 추가/수정**

`App/frontend/src/dashboard/libs/utils/dashboardTaskUtils.ts`의 `formatDashboardDueDate`를 아래로 교체:

```ts
export function formatDashboardDueDate(dueDate: string | null | undefined): string {
  if (!dueDate) return "미정";
  const [, month, day] = dueDate.slice(0, 10).split("-");
  return month && day ? `${month}.${day}` : dueDate;
}
```

같은 파일의 `daysUntilDue` 함수 바로 다음에 두 함수를 추가:

```ts
/** ISO 날짜/일시 문자열부터 지금까지 경과한 시간을 "일" 단위로 내림 계산한다.
 * 캘린더 날짜 경계가 아니라 실제 경과 시간 기준(체류시간 근사 — 백엔드 ML 피처
 * hours_in_current_status와 동일한 방식)이라 "3일 이상"류 임계값 판정에 적합하다. */
export function daysSince(dateStr: string | null | undefined): number | null {
  if (!dateStr) return null;
  const past = new Date(dateStr);
  if (Number.isNaN(past.getTime())) return null;
  return Math.max(0, Math.floor((Date.now() - past.getTime()) / 86400000));
}

/** "오늘"/"어제"/"n일 전"(30일 이상이면 MM.DD) — 캘린더 날짜 경계 기준 상대 날짜 표기.
 * daysSince와 달리 사람이 읽는 라벨이라 자정 기준으로 날짜를 비교한다. */
export function formatRelativeDate(dateStr: string | null | undefined): string {
  if (!dateStr) return "미정";
  const past = new Date(dateStr);
  if (Number.isNaN(past.getTime())) return "미정";
  const pastDay = new Date(past.getFullYear(), past.getMonth(), past.getDate()).getTime();
  const today = new Date();
  const todayDay = new Date(today.getFullYear(), today.getMonth(), today.getDate()).getTime();
  const diffDays = Math.round((todayDay - pastDay) / 86400000);
  if (diffDays <= 0) return "오늘";
  if (diffDays === 1) return "어제";
  if (diffDays < 30) return `${diffDays}일 전`;
  return formatDashboardDueDate(dateStr);
}
```

- [ ] **Step 5: 테스트 통과 확인**

Run: `pnpm exec vitest run src/dashboard/libs/utils/dashboardTaskUtils.test.ts`
Expected: PASS (전체 케이스)

- [ ] **Step 6: 커밋**

```bash
git add App/frontend/src/dashboard/libs/types/dashboard.ts \
        App/frontend/src/dashboard/libs/utils/dashboardTaskUtils.ts \
        App/frontend/src/dashboard/libs/utils/dashboardTaskUtils.test.ts
git commit -m "feat(dashboard): 업무 날짜 타입/유틸(daysSince, formatRelativeDate) 추가"
```

---

## Task 4: 프론트엔드 — AI 어시스턴트 오픈 이벤트 공용 헬퍼

**Files:**
- Create: `App/frontend/src/ai/libs/utils/openAIAssistant.ts`
- Test: `App/frontend/src/ai/libs/utils/openAIAssistant.test.ts`

**Interfaces:**
- Produces: `OPEN_AI_ASSISTANT_EVENT: string`, `openAIAssistant(question?: string): void`,
  `OpenAIAssistantEventDetail`(타입) — Task 5(AppShell/AIAssistant 배선)와 Task 6(ProgressPage)/
  Task 7(BlockersPage)의 AI 버튼에서 사용.

- [ ] **Step 1: 실패하는 테스트 작성**

새 파일 `App/frontend/src/ai/libs/utils/openAIAssistant.test.ts`:

```ts
import { describe, expect, it, vi } from "vitest";
import { OPEN_AI_ASSISTANT_EVENT, openAIAssistant } from "./openAIAssistant";

describe("openAIAssistant", () => {
  it("dispatches a CustomEvent carrying the question", () => {
    const handler = vi.fn();
    window.addEventListener(OPEN_AI_ASSISTANT_EVENT, handler);

    openAIAssistant("블로커 해결 방법 추천해줘");

    expect(handler).toHaveBeenCalledTimes(1);
    const event = handler.mock.calls[0][0] as CustomEvent<{ question?: string; requestId: number }>;
    expect(event.detail.question).toBe("블로커 해결 방법 추천해줘");
    expect(typeof event.detail.requestId).toBe("number");

    window.removeEventListener(OPEN_AI_ASSISTANT_EVENT, handler);
  });

  it("dispatches with an undefined question when called without an argument", () => {
    const handler = vi.fn();
    window.addEventListener(OPEN_AI_ASSISTANT_EVENT, handler);

    openAIAssistant();

    const event = handler.mock.calls[0][0] as CustomEvent<{ question?: string; requestId: number }>;
    expect(event.detail.question).toBeUndefined();

    window.removeEventListener(OPEN_AI_ASSISTANT_EVENT, handler);
  });

  it("uses a different requestId on each call", () => {
    const handler = vi.fn();
    window.addEventListener(OPEN_AI_ASSISTANT_EVENT, handler);

    openAIAssistant("질문 A");
    openAIAssistant("질문 A");

    const first = (handler.mock.calls[0][0] as CustomEvent<{ requestId: number }>).detail.requestId;
    const second = (handler.mock.calls[1][0] as CustomEvent<{ requestId: number }>).detail.requestId;
    expect(second).not.toBe(first);

    window.removeEventListener(OPEN_AI_ASSISTANT_EVENT, handler);
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `pnpm exec vitest run src/ai/libs/utils/openAIAssistant.test.ts`
Expected: FAIL — 모듈이 없어 임포트 에러.

- [ ] **Step 3: 헬퍼 구현**

새 파일 `App/frontend/src/ai/libs/utils/openAIAssistant.ts`:

```ts
export const OPEN_AI_ASSISTANT_EVENT = "workflow-ai:open-ai-assistant";

export interface OpenAIAssistantEventDetail {
  question?: string;
  requestId: number;
}

/** AI 어시스턴트 패널을 열고, question이 있으면 자동으로 질문을 전송하게 한다.
 * requestId는 동일한 문구를 다시 요청해도 AIAssistant의 useEffect가 항상 반응하도록
 * 매 호출마다 새 값을 부여한다 (question 텍스트만으로는 dependency가 안 바뀔 수 있음). */
export function openAIAssistant(question?: string): void {
  const detail: OpenAIAssistantEventDetail = { question, requestId: Date.now() + Math.random() };
  window.dispatchEvent(new CustomEvent<OpenAIAssistantEventDetail>(OPEN_AI_ASSISTANT_EVENT, { detail }));
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `pnpm exec vitest run src/ai/libs/utils/openAIAssistant.test.ts`
Expected: PASS

- [ ] **Step 5: 커밋**

```bash
git add App/frontend/src/ai/libs/utils/openAIAssistant.ts \
        App/frontend/src/ai/libs/utils/openAIAssistant.test.ts
git commit -m "feat(ai): AI 어시스턴트 자동 질문 오픈 헬퍼 추가"
```

---

## Task 5: 프론트엔드 — `AIAssistant`/`AppShell`/`DashboardView` 배선

**Files:**
- Modify: `App/frontend/src/ai/screen/AIAssistant.tsx`
- Modify: `App/frontend/src/global/component/layout/AppShell.tsx`
- Modify: `App/frontend/src/dashboard/screen/DashboardView.tsx`
- Test: `App/frontend/src/ai/screen/AIAssistant.test.tsx`

**Interfaces:**
- Consumes: Task 4의 `OPEN_AI_ASSISTANT_EVENT`, `openAIAssistant`.
- Produces: `AIAssistant`가 받는 `pendingQuestion?: { text: string; requestId: number } | null` prop.

- [ ] **Step 1: 실패하는 테스트 작성**

`App/frontend/src/ai/screen/AIAssistant.test.tsx`의 `describe("AIAssistant", ...)` 블록 안, 마지막
`it(...)` 다음에 테스트를 추가:

```tsx
  it("auto-sends the pending question when it changes", async () => {
    vi.mocked(apiFetch).mockResolvedValue({
      answer: "해결 방법 추천 답변",
      sources: [],
    });

    const { rerender } = render(<AIAssistant onClose={() => {}} pendingQuestion={null} />);
    rerender(<AIAssistant onClose={() => {}} pendingQuestion={{ text: "블로커 해결 방법 추천해줘", requestId: 1 }} />);

    await waitFor(() => expect(screen.getByText("블로커 해결 방법 추천해줘")).toBeInTheDocument());
    await waitFor(() => expect(screen.getByText("해결 방법 추천 답변")).toBeInTheDocument());
  });
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `pnpm exec vitest run src/ai/screen/AIAssistant.test.tsx`
Expected: FAIL — `pendingQuestion` prop을 몰라 자동 전송되지 않음(질문 텍스트가 렌더되지 않음).

- [ ] **Step 3: `AIAssistant`에 `pendingQuestion` 지원 추가**

`App/frontend/src/ai/screen/AIAssistant.tsx`의 컴포넌트 선언부(1번째 줄 부근)를 아래로 교체:

```tsx
export function AIAssistant({
  onClose,
  pendingQuestion,
}: {
  onClose: () => void;
  pendingQuestion?: { text: string; requestId: number } | null;
}) {
```

`const send = (text: string) => { ... };` 정의 바로 다음(스크롤 useEffect와는 별개)에 새 `useEffect`를 추가:

```tsx
  useEffect(() => {
    if (pendingQuestion?.text) {
      send(pendingQuestion.text);
    }
    // pendingQuestion.requestId가 바뀔 때만 반응 — send는 렌더마다 새로 만들어지는 클로저라
    // 의존성에 넣으면 무한 재실행되므로 의도적으로 제외한다.
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [pendingQuestion?.requestId]);
```

- [ ] **Step 4: `AppShell`에서 `pendingQuestion` 상태 배선**

`App/frontend/src/global/component/layout/AppShell.tsx`를 아래처럼 수정한다.

import 블록의 `import { AIAssistant } from "../../../ai/screen/AIAssistant";` 다음 줄에 추가:
```tsx
import { OPEN_AI_ASSISTANT_EVENT, type OpenAIAssistantEventDetail } from "../../../ai/libs/utils/openAIAssistant";
```
그리고 파일 상단의 `const OPEN_AI_ASSISTANT_EVENT = "workflow-ai:open-ai-assistant";` 줄은 삭제한다
(이제 공용 헬퍼에서 가져온다).

`const [aiOpen, setAIOpen] = useState(false);` 다음 줄에 추가:
```tsx
  const [pendingQuestion, setPendingQuestion] = useState<{ text: string; requestId: number } | null>(null);
```

기존
```tsx
  useEffect(() => {
    const open = () => setAIOpen(true);
    window.addEventListener(OPEN_AI_ASSISTANT_EVENT, open);
    return () => window.removeEventListener(OPEN_AI_ASSISTANT_EVENT, open);
  }, []);
```
를 아래로 교체:
```tsx
  useEffect(() => {
    const open = (event: Event) => {
      setAIOpen(true);
      const detail = (event as CustomEvent<OpenAIAssistantEventDetail>).detail;
      if (detail?.question) {
        setPendingQuestion({ text: detail.question, requestId: detail.requestId });
      }
    };
    window.addEventListener(OPEN_AI_ASSISTANT_EVENT, open);
    return () => window.removeEventListener(OPEN_AI_ASSISTANT_EVENT, open);
  }, []);
```

기존
```tsx
      {aiOpen && (
        <>
          <div className="fixed inset-0 bg-black/10 z-40" onClick={() => setAIOpen(false)} />
          <AIAssistant onClose={() => setAIOpen(false)} />
        </>
      )}
```
를 아래로 교체:
```tsx
      {aiOpen && (
        <>
          <div className="fixed inset-0 bg-black/10 z-40" onClick={() => setAIOpen(false)} />
          <AIAssistant onClose={() => setAIOpen(false)} pendingQuestion={pendingQuestion} />
        </>
      )}
```

- [ ] **Step 5: `DashboardView`의 중복 이벤트 상수 제거**

`App/frontend/src/dashboard/screen/DashboardView.tsx`의
`import { resolveMemberDisplay } from "../libs/utils/memberDisplay";` 다음 줄에 추가:
```tsx
import { openAIAssistant } from "../../ai/libs/utils/openAIAssistant";
```
`const OPEN_AI_ASSISTANT_EVENT = "workflow-ai:open-ai-assistant";` 줄은 삭제한다.

`quickActions` 배열의
```tsx
    { label: "AI 어시스턴트", icon: Sparkles, color: "#F59E0B", onClick: () => window.dispatchEvent(new Event(OPEN_AI_ASSISTANT_EVENT)) },
```
를 아래로 교체:
```tsx
    { label: "AI 어시스턴트", icon: Sparkles, color: "#F59E0B", onClick: () => openAIAssistant() },
```

- [ ] **Step 6: 테스트 통과 확인**

Run: `pnpm exec vitest run src/ai/screen/AIAssistant.test.tsx`
Expected: PASS (4개 기존 테스트 + 신규 1개)

Run(타입 체크 겸): `pnpm build`
Expected: 빌드 성공(타입 에러 없음)

- [ ] **Step 7: 커밋**

```bash
git add App/frontend/src/ai/screen/AIAssistant.tsx \
        App/frontend/src/ai/screen/AIAssistant.test.tsx \
        App/frontend/src/global/component/layout/AppShell.tsx \
        App/frontend/src/dashboard/screen/DashboardView.tsx
git commit -m "feat(ai): AppShell/DashboardView가 공용 오픈 헬퍼로 AI 패널에 질문 자동 전송"
```

---

## Task 6: ProgressPage.tsx — '진행률 보고서 생성' 버튼

**Files:**
- Modify: `App/frontend/src/dashboard/screen/detail/ProgressPage.tsx`

**Interfaces:**
- Consumes: `openAIAssistant` (Task 4).

- [ ] **Step 1: import 추가**

`App/frontend/src/dashboard/screen/detail/ProgressPage.tsx` 상단 import를 수정한다.
`import { Calendar, CheckCircle2, CheckSquare, Sparkles, TrendingUp } from "lucide-react";`를
```tsx
import { Calendar, CheckCircle2, CheckSquare, FileText, TrendingUp } from "lucide-react";
```
로 교체(버튼 아이콘을 `Sparkles`→`FileText`로 바꾸므로 `Sparkles`는 더 이상 쓰이지 않는지 확인 —
파일 내 "AI 완료 예측" 섹션(약 88번째 줄)에서 여전히 `<Sparkles .../>`를 직접 쓰므로 실제로는
`Sparkles`도 계속 필요하다. 최종 import는 아래로 한다):
```tsx
import { Calendar, CheckCircle2, CheckSquare, FileText, Sparkles, TrendingUp } from "lucide-react";
```

`import { AIBox } from "../../../ai/components/AIBox";` 다음 줄에 추가:
```tsx
import { openAIAssistant } from "../../../ai/libs/utils/openAIAssistant";
```

- [ ] **Step 2: 버튼 문구/기능 교체**

기존
```tsx
        <button onClick={() => navigate("/dashboard/dash-progress")} className="flex items-center gap-1.5 px-4 py-2 text-sm font-medium text-white rounded-lg" style={{ background: "linear-gradient(135deg,#7048E8,#4F6EF7)" }}>
          <Sparkles className="w-4 h-4" /> AI 지연 분석
        </button>
```
를 아래로 교체:
```tsx
        <button
          onClick={() => openAIAssistant(`현재 프로젝트 진행률을 요약한 보고서를 작성해줘. 전체 ${totalTasks}건 중 ${doneTasks}건 완료(${progressPercent}%), 마일스톤 ${milestones.length}건.`)}
          className="flex items-center gap-1.5 px-4 py-2 text-sm font-medium text-white rounded-lg"
          style={{ background: "linear-gradient(135deg,#7048E8,#4F6EF7)" }}
        >
          <FileText className="w-4 h-4" /> 진행률 보고서 생성
        </button>
```

- [ ] **Step 3: 타입 체크**

Run (in `App/frontend`): `pnpm build`
Expected: 빌드 성공. (이 페이지는 기존에 테스트가 없어 새 RTL 테스트는 추가하지 않는다 — Task 10에서
브라우저로 직접 확인한다.)

- [ ] **Step 4: 커밋**

```bash
git add App/frontend/src/dashboard/screen/detail/ProgressPage.tsx
git commit -m "feat(dashboard): 진행률 분석 페이지 버튼을 '진행률 보고서 생성'으로 교체"
```

---

## Task 7: BlockersPage.tsx — 평균 지연 / 태그 색상 / 체류일 배지 / AI 버튼

**Files:**
- Modify: `App/frontend/src/dashboard/screen/detail/BlockersPage.tsx`

**Interfaces:**
- Consumes: `daysSince`, `daysUntilDue`(기존) (Task 3), `openAIAssistant`(Task 4),
  `task.updatedAt`/`progress.delayRisks[].dueDate`(Task 1/2 백엔드 필드).

- [ ] **Step 1: import 수정**

기존
```tsx
import {
  daysUntilDue,
  formatDashboardDueDate,
  normalizePriority,
  normalizeTaskStatus,
  sourceLabel,
  taskAssignee,
} from "../../libs/utils/dashboardTaskUtils";
```
를 아래로 교체:
```tsx
import {
  daysSince,
  daysUntilDue,
  formatDashboardDueDate,
  normalizePriority,
  normalizeTaskStatus,
  sourceLabel,
  taskAssignee,
} from "../../libs/utils/dashboardTaskUtils";
```

`import { useAuth } from "../../../global/hooks/useAuth";` 다음 줄에 추가:
```tsx
import { openAIAssistant } from "../../../ai/libs/utils/openAIAssistant";
```

- [ ] **Step 2: 평균 지연 계산 추가, 미사용 `dueSoonCount` 제거**

기존
```tsx
  const blockedTasks = tasks.filter(task => normalizeTaskStatus(task.status) === "blocked");
  const highPriorityCount = blockedTasks.filter(task => normalizePriority(task.priority) === "high").length;
  const dueSoonCount = blockedTasks.filter(task => {
    const days = daysUntilDue(task.dueDate);
    return days != null && days <= 7;
  }).length;
  const riskTaskIds = new Set(progress?.delayRisks.map(risk => risk.taskId) ?? []);
```
를 아래로 교체:
```tsx
  const blockedTasks = tasks.filter(task => normalizeTaskStatus(task.status) === "blocked");
  const highPriorityCount = blockedTasks.filter(task => normalizePriority(task.priority) === "high").length;
  const riskTaskIds = new Set(progress?.delayRisks.map(risk => risk.taskId) ?? []);
  const overdueRiskDelayDays = (progress?.delayRisks ?? [])
    .map(risk => daysUntilDue(risk.dueDate))
    .filter((days): days is number => days != null && days < 0)
    .map(days => -days);
  const averageDelayDays = overdueRiskDelayDays.length === 0
    ? 0
    : Math.round(overdueRiskDelayDays.reduce((sum, d) => sum + d, 0) / overdueRiskDelayDays.length);
```

- [ ] **Step 3: '7일 내 마감' 카드 → '평균 지연' 카드**

기존
```tsx
        <DetailStatCard label="7일 내 마감" value={loading ? "..." : dueSoonCount} sub="일정 영향 가능" color="#F59E0B" icon={Clock} />
```
를 아래로 교체:
```tsx
        <DetailStatCard
          label="평균 지연"
          value={loading ? "..." : `${averageDelayDays}일`}
          sub={overdueRiskDelayDays.length === 0 ? "지연 대상 없음" : `주의·위험 ${overdueRiskDelayDays.length}건 기준`}
          color="#F97316"
          icon={Clock}
        />
```

- [ ] **Step 4: 지연 위험 태그 색상 변경**

기존
```tsx
                      {isRisk && <span className="text-[10px] font-semibold px-2 py-0.5 rounded-full bg-red-100 text-red-700 border border-red-200">지연 위험</span>}
```
를 아래로 교체:
```tsx
                      {isRisk && <span className="text-[10px] font-semibold px-2 py-0.5 rounded-full bg-orange-100 text-orange-700 border border-orange-200">지연 위험</span>}
```

- [ ] **Step 5: D-Day 배지 → 'N일째 지속' 배지**

기존
```tsx
              const assignee = taskAssignee(task, index);
              const priority = normalizePriority(task.priority);
              const days = daysUntilDue(task.dueDate);
              const isRisk = riskTaskIds.has(task.id);
```
를 아래로 교체:
```tsx
              const assignee = taskAssignee(task, index);
              const priority = normalizePriority(task.priority);
              const isRisk = riskTaskIds.has(task.id);
              const dwellDays = daysSince(task.updatedAt) ?? 0;
```

기존
```tsx
                <span className="text-xs font-medium px-2.5 py-1 rounded-lg bg-red-100 text-red-700 shrink-0 whitespace-nowrap">
                  {days == null ? "마감 미정" : days < 0 ? `D+${Math.abs(days)}` : `D-${days}`}
                </span>
```
를 아래로 교체:
```tsx
                <span className="text-xs font-medium px-2.5 py-1 rounded-lg bg-red-100 text-red-700 shrink-0 whitespace-nowrap">
                  {dwellDays}일째 지속
                </span>
```

- [ ] **Step 6: 'AI 분석 보기' 버튼 → 'AI 해결 방법 추천' 버튼**

기존
```tsx
                  <button onClick={() => navigate("/dashboard/dash-progress")} className="flex items-center gap-1.5 px-3 py-1.5 text-xs font-semibold rounded-lg ml-auto transition-opacity hover:opacity-80" style={{ background: "rgba(112,72,232,0.12)", color: "#7048E8" }}>
                    <Sparkles className="w-3.5 h-3.5" /> AI 분석 보기
                  </button>
```
를 아래로 교체:
```tsx
                  <button
                    onClick={() => openAIAssistant(`'${task.title}' 업무가 블로커 상태입니다. 설명: ${task.description || "설명 없음"}. 이 블로커를 해결할 방법을 추천해줘.`)}
                    className="flex items-center gap-1.5 px-3 py-1.5 text-xs font-semibold rounded-lg ml-auto transition-opacity hover:opacity-80"
                    style={{ background: "rgba(112,72,232,0.12)", color: "#7048E8" }}
                  >
                    <Sparkles className="w-3.5 h-3.5" /> AI 해결 방법 추천
                  </button>
```

- [ ] **Step 7: 타입 체크**

Run: `pnpm build`
Expected: 빌드 성공.

- [ ] **Step 8: 커밋**

```bash
git add App/frontend/src/dashboard/screen/detail/BlockersPage.tsx
git commit -m "feat(dashboard): 블로커 관리 페이지 - 평균 지연/태그 색상/체류일/AI 버튼 교체"
```

---

## Task 8: InProgressPage.tsx — 업데이트 필요 / 지연 위험 / D-Day / 날짜 정보

**Files:**
- Modify: `App/frontend/src/dashboard/screen/detail/InProgressPage.tsx`

**Interfaces:**
- Consumes: `daysSince`, `formatRelativeDate`, `daysUntilDue`(기존), `formatDashboardDueDate`(기존) (Task 3),
  `progress.projectDeadline`(Task 2), `task.createdAt`/`task.updatedAt`(Task 1).

- [ ] **Step 1: import 수정**

기존
```tsx
import {
  daysUntilDue,
  formatDashboardDueDate,
  normalizePriority,
  normalizeTaskStatus,
  sourceLabel,
  taskAssignee,
} from "../../libs/utils/dashboardTaskUtils";
```
를 아래로 교체:
```tsx
import {
  daysSince,
  daysUntilDue,
  formatDashboardDueDate,
  formatRelativeDate,
  normalizePriority,
  normalizeTaskStatus,
  sourceLabel,
  taskAssignee,
} from "../../libs/utils/dashboardTaskUtils";
```

- [ ] **Step 2: 카드 계산값 추가**

기존
```tsx
  const inProgressTasks = tasks.filter(task => normalizeTaskStatus(task.status) === "inprogress");
  const highPriorityCount = inProgressTasks.filter(task => normalizePriority(task.priority) === "high").length;
  const dueSoonCount = inProgressTasks.filter(task => {
    const days = daysUntilDue(task.dueDate);
    return days != null && days <= 7;
  }).length;
  const riskTaskIds = new Set(progress?.delayRisks.map(risk => risk.taskId) ?? []);
```
를 아래로 교체:
```tsx
  const inProgressTasks = tasks.filter(task => normalizeTaskStatus(task.status) === "inprogress");
  const riskTaskIds = new Set(progress?.delayRisks.map(risk => risk.taskId) ?? []);
  const riskResultByTaskId = new Map((progress?.delayRisks ?? []).map(risk => [risk.taskId, risk.result]));
  const needsUpdateCount = inProgressTasks.filter(task => (daysSince(task.updatedAt) ?? 0) >= 3).length;
  const dangerCount = inProgressTasks.filter(task => riskResultByTaskId.get(task.id) === "위험").length;
  const projectDeadline = progress?.projectDeadline ?? null;
  const daysToDeadline = daysUntilDue(projectDeadline);
```

(`highPriorityCount`는 더 이상 카드에서 쓰이지 않으므로 제거한다. `AlertTriangle`/`AlertCircle` 아이콘은
계속 다른 카드에서 쓰이므로 import는 그대로 둔다.)

- [ ] **Step 3: 상단 통계 카드 4개 교체**

기존
```tsx
      <div className="grid grid-cols-4 gap-3">
        <DetailStatCard label="진행 중" value={loading ? "..." : inProgressTasks.length} sub="활성 업무" color="#3B5BDB" icon={Clock} />
        <DetailStatCard label="높은 우선순위" value={loading ? "..." : highPriorityCount} sub="우선 확인" color="#F59E0B" icon={AlertTriangle} />
        <DetailStatCard label="7일 내 마감" value={loading ? "..." : dueSoonCount} sub="일정 확인" color="#EF4444" icon={AlertCircle} />
        <DetailStatCard label="AI 위험 감지" value={loading ? "..." : riskTaskIds.size} sub="예측 결과" color="#7048E8" icon={Calendar} />
      </div>
```
를 아래로 교체:
```tsx
      <div className="grid grid-cols-4 gap-3">
        <DetailStatCard label="진행 중" value={loading ? "..." : inProgressTasks.length} sub="활성 업무" color="#3B5BDB" icon={Clock} />
        <DetailStatCard label="업데이트 필요" value={loading ? "..." : needsUpdateCount} sub="3일 이상 정체" color="#F59E0B" icon={RefreshCw} />
        <DetailStatCard label="지연 위험" value={loading ? "..." : dangerCount} sub="AI 예측 '위험'" color="#EF4444" icon={AlertCircle} />
        <DetailStatCard
          label="D-Day"
          value={loading ? "..." : daysToDeadline == null ? "-" : daysToDeadline < 0 ? `D+${Math.abs(daysToDeadline)}` : `D-${daysToDeadline}`}
          sub={projectDeadline ? formatDashboardDueDate(projectDeadline) : "마감일 미설정"}
          color="#7048E8"
          icon={Calendar}
        />
      </div>
```

(`AlertTriangle` import는 이 카드 행에서는 안 쓰이지만, 파일 하단의 업무 카드 액션 버튼
`<AlertTriangle className="w-3.5 h-3.5 text-red-500" /> 블로커 전환`에서 계속 쓰이므로 import 목록은 그대로 둔다.)

- [ ] **Step 4: 업무 카드 우측 날짜 정보 확장**

기존
```tsx
                  <div className="flex items-center gap-2 shrink-0">
                    <span className={`text-xs font-semibold bg-muted px-2 py-1 rounded-lg ${isDueSoon ? "text-amber-600" : "text-foreground"}`}>
                      마감 {formatDashboardDueDate(task.dueDate)}
                    </span>
                  </div>
```
를 아래로 교체:
```tsx
                  <div className="flex flex-col items-end gap-1 shrink-0 text-[10px] text-muted-foreground">
                    <span>시작 {formatDashboardDueDate(task.createdAt)}</span>
                    <span>업데이트 {formatRelativeDate(task.updatedAt)}</span>
                    <span className={`font-semibold bg-muted px-2 py-1 rounded-lg ${isDueSoon ? "text-amber-600" : "text-foreground"}`}>
                      마감 {formatDashboardDueDate(task.dueDate)}
                    </span>
                  </div>
```

- [ ] **Step 5: 타입 체크**

Run: `pnpm build`
Expected: 빌드 성공.

- [ ] **Step 6: 커밋**

```bash
git add App/frontend/src/dashboard/screen/detail/InProgressPage.tsx
git commit -m "feat(dashboard): 진행 중 업무 페이지 - 업데이트 필요/지연 위험/D-Day/날짜 정보 추가"
```

---

## Task 9: DashProgressPage.tsx — 계획 대비 / 지연 업무 / 마감 D-day / 상태 범례

**Files:**
- Modify: `App/frontend/src/dashboard/screen/detail/DashProgressPage.tsx`

**Interfaces:**
- Consumes: `daysUntilDue`(기존, Task 3), `progress.projectDeadline`/`progress.projectCreatedAt`(Task 2).

- [ ] **Step 1: import 수정**

기존
```tsx
import { AlertTriangle, Calendar, Clock, FileText, Loader2, ShieldAlert, Sparkles, TrendingUp } from "lucide-react";
```
를 아래로 교체:
```tsx
import { AlertTriangle, Calendar, Clock, FileText, Loader2, ShieldAlert, Sparkles, Target, TrendingUp } from "lucide-react";
```

기존
```tsx
import { formatDashboardDueDate } from "../../libs/utils/dashboardTaskUtils";
```
를 아래로 교체:
```tsx
import { daysUntilDue, formatDashboardDueDate } from "../../libs/utils/dashboardTaskUtils";
```

- [ ] **Step 2: 계획 대비 / D-day 계산 추가**

`const milestones = progress?.milestones ?? [];` 다음 줄에 추가:
```tsx
  const projectDeadline = progress?.projectDeadline ?? null;
  const projectCreatedAt = progress?.projectCreatedAt ?? null;
  const daysToDeadline = daysUntilDue(projectDeadline);

  let planVsActualValue = "-";
  let planVsActualSub = "마감일 미설정";
  let planVsActualColor = "#8892A4";
  if (projectDeadline && projectCreatedAt) {
    const start = new Date(`${projectCreatedAt}T00:00:00`).getTime();
    const end = new Date(`${projectDeadline}T00:00:00`).getTime();
    const totalSpan = end - start;
    const expectedPercent = totalSpan <= 0
      ? 100
      : Math.min(100, Math.max(0, Math.round(((Date.now() - start) / totalSpan) * 100)));
    const diff = expectedPercent - progressPercent;
    planVsActualSub = `예상 ${expectedPercent}% · 실제 ${progressPercent}%`;
    if (diff > 0) {
      planVsActualValue = `-${diff}%p`;
      planVsActualColor = "#EF4444";
    } else if (diff < 0) {
      planVsActualValue = `+${-diff}%p`;
      planVsActualColor = "#10B981";
    } else {
      planVsActualValue = "0%p";
    }
  }
```

- [ ] **Step 3: 상단 통계 카드 4개 → 5개로 교체**

기존
```tsx
      <div className="grid grid-cols-4 gap-3">
        <DetailStatCard label="전체 완료율" value={loading ? "..." : `${progressPercent}%`} sub={loading ? "불러오는 중" : `${doneTasks} / ${totalTasks} 완료`} color="#3B5BDB" icon={TrendingUp} />
        <DetailStatCard label="미완료 업무" value={loading ? "..." : `${Math.max(totalTasks - doneTasks, 0)}개`} sub="완료 전 상태" color="#F59E0B" icon={AlertTriangle} />
        <DetailStatCard label="지연 위험 업무" value={loading ? "..." : `${delayRisks.length}개`} sub={hasPredictions ? "AI 분석 결과" : "분석 필요"} color="#EF4444" icon={Clock} />
        <DetailStatCard label="마일스톤" value={loading ? "..." : `${milestones.length}개`} sub="등록된 일정" color="#7048E8" icon={Calendar} />
      </div>
```
를 아래로 교체:
```tsx
      <div className="grid grid-cols-5 gap-3">
        <DetailStatCard label="전체 완료율" value={loading ? "..." : `${progressPercent}%`} sub={loading ? "불러오는 중" : `${doneTasks} / ${totalTasks} 완료`} color="#3B5BDB" icon={TrendingUp} />
        <DetailStatCard label="계획 대비" value={loading ? "..." : planVsActualValue} sub={loading ? "불러오는 중" : planVsActualSub} color={planVsActualColor} icon={Target} />
        <DetailStatCard label="지연 업무" value={loading ? "..." : `${delayRisks.length}개`} sub={hasPredictions ? "AI 분석 결과" : "분석 필요"} color="#EF4444" icon={Clock} />
        <DetailStatCard
          label="마감 D-day"
          value={loading ? "..." : daysToDeadline == null ? "-" : daysToDeadline < 0 ? `D+${Math.abs(daysToDeadline)}` : `D-${daysToDeadline}`}
          sub={projectDeadline ? formatDashboardDueDate(projectDeadline) : "마감일 미설정"}
          color="#7048E8"
          icon={Calendar}
        />
        <div className="bg-card rounded-xl p-4 shadow-sm border border-border">
          <div className="text-xs font-medium text-foreground mb-2">상태 범례</div>
          <div className="space-y-1.5">
            {[
              { label: "정상", color: "#10B981" },
              { label: "주의", color: "#F59E0B" },
              { label: "위험", color: "#EF4444" },
            ].map(item => (
              <div key={item.label} className="flex items-center gap-1.5 text-[10px] text-muted-foreground">
                <span className="w-2 h-2 rounded-full shrink-0" style={{ background: item.color }} />
                <span>{item.label}</span>
              </div>
            ))}
          </div>
        </div>
      </div>
```

- [ ] **Step 4: 타입 체크**

Run: `pnpm build`
Expected: 빌드 성공.

- [ ] **Step 5: 커밋**

```bash
git add App/frontend/src/dashboard/screen/detail/DashProgressPage.tsx
git commit -m "feat(dashboard): 전체 진행률 페이지 - 계획 대비/지연 업무/마감 D-day/상태 범례 교체"
```

---

## Task 10: 통합 검증 (백엔드 빌드 + 프론트 빌드/테스트 + 브라우저 수동 확인)

**Files:** 없음(검증 전용)

- [ ] **Step 1: 백엔드 전체 테스트**

Run (in `App/backend_spring`): `./gradlew test`
Expected: BUILD SUCCESSFUL, 모든 테스트 통과.

- [ ] **Step 2: 프론트엔드 전체 테스트 + 빌드**

Run (in `App/frontend`): `pnpm test`
Expected: 모든 테스트 통과(신규 4개 파일 + 기존 스위트).

Run: `pnpm build`
Expected: 빌드 성공, 타입 에러 없음.

- [ ] **Step 3: 개발 서버로 4개 화면 수동 확인**

`run` 스킬(or `.claude/launch.json` 기준 dev 서버)로 프론트엔드를 띄우고, 로그인 후 아래를 브라우저에서 직접 확인한다:

- `/dashboard/progress` — "진행률 보고서 생성" 버튼 클릭 → AI 어시스턴트 패널이 열리고 질문이 자동 전송되는지.
- `/dashboard/blockers` — "평균 지연" 카드 값, "지연 위험" 태그가 주황색인지, 업무 카드의 "N일째 지속"
  배지, "AI 해결 방법 추천" 버튼 클릭 시 자동 질문 전송.
- `/dashboard/inprogress` — "업데이트 필요"/"지연 위험"/"D-Day" 카드 값, 업무 카드에 시작일/업데이트/마감일
  세 줄이 보이는지.
- `/dashboard/dash-progress` — "계획 대비"/"지연 업무"/"마감 D-day" 카드와 "상태 범례" 카드(5개 카드 한 줄).

이상이 없으면 완료로 표시한다. 문제가 있으면 해당 Task로 돌아가 수정한다.

---
