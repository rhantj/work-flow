# 새 프로젝트용 docker-compose.yml 초안
# 사용법: cp .env.example .env  →  docker compose up -d
#
# 주의: 아래 ${POSTGRES_PASSWORD:-root} 등의 기본값은 로컬 개발 편의를 위한 예시일 뿐이다.
# 서버(OCI 등)에 배포할 땐 반드시 .env에 실제 값(POSTGRES_PASSWORD, JWT_SECRET 등)을 채워서
# 이 기본값이 그대로 쓰이지 않게 할 것.
services:
  db:
    # pgvector 확장이 포함된 이미지여야 한다. db/init/11_pre_baseline_backfill.sql이
    # CREATE EXTENSION vector를 실행하므로, 순정 postgres:17에서는 빈 볼륨 초기화가
    # 그 지점에서 실패한다(운영 compose는 처음부터 이 이미지를 쓴다).
    image: pgvector/pgvector:pg17
    container_name: workflow-db
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-workflow}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-root}
    ports:
      - '${DB_HOST_PORT:-5432}:5432'
    volumes:
      - db-data:/var/lib/postgresql/data
      # 최초 기동(볼륨이 비어있을 때)에만 자동 실행되는 스키마 초기화 스크립트.
      # backend_spring/src/main/resources/db/init 참고.
      - ./backend_spring/src/main/resources/db/init:/docker-entrypoint-initdb.d:ro
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER:-postgres}']
      interval: 5s
      timeout: 3s
      retries: 10

  redis:
    # 순수 Redis. 어시스턴트 체크포인터가 InMemorySaver로 바뀌어 RediSearch/RedisJSON
    # 모듈이 더 이상 필요 없다. 캐시·스트림·스레드 소유권 등 나머지 사용은 순수 Redis 명령뿐.
    image: redis:7-alpine
    container_name: workflow-redis
    command:
      - redis-server
      # AOF/RDB를 redis-data 볼륨(/data)에 쓰도록 명시한다(미지정 시 볼륨 밖에 쓰여 유실).
      - --dir
      - /data
      # 호스트 포트는 127.0.0.1로만 노출돼 docker 내부망 한정 접근이므로 protected-mode를 끈다
      # (다른 컨테이너 fastapi/spring 연결이 DENIED되지 않게).
      - --protected-mode
      - 'no'
      - --appendonly
      - 'yes'
      - --appendfsync
      - everysec
      - --maxmemory
      - ${REDIS_MAXMEMORY:-512mb}
      - --maxmemory-policy
      - noeviction
      - --auto-aof-rewrite-percentage
      - '50'
      - --auto-aof-rewrite-min-size
      - 16mb
    ports:
      - '127.0.0.1:${REDIS_HOST_PORT:-6379}:6379'
    volumes:
      - redis-data:/data
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 5s
      timeout: 3s
      retries: 10

  kafka:
    image: apache/kafka:3.8.0 # KRaft 모드 내장 — zookeeper 불필요
    container_name: workflow-kafka
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: broker,controller
      KAFKA_LISTENERS: PLAINTEXT://:9092,CONTROLLER://:9093
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@kafka:9093
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: 'true'
    ports:
      - '9092:9092'

  backend-spring:
    build: ./backend_spring
    container_name: workflow-backend-spring
    environment:
      TZ: Asia/Seoul # LocalDateTime.now() 등이 KST(UTC+9) 벽시계 값으로 나오도록 JVM 타임존 고정
      DB_HOST: db # application.properties에서 이 이름으로 참조
      REDIS_HOST: redis
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092 # KafkaConfig.java의 실제 프로퍼티 키에 맞춰 이름 조정
      POSTGRES_DB: ${POSTGRES_DB:-workflow}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-root}
      # 기본은 compose 안의 로컬 db 서비스에 연결한다.
      # Supabase(원격)에 직접 연결하려면 .env의 SPRING_DATASOURCE_* 값으로 덮어쓴다.
      SPRING_DATASOURCE_URL: ${SPRING_DATASOURCE_URL:-jdbc:postgresql://db:5432/workflow}
      SPRING_DATASOURCE_USERNAME: ${SPRING_DATASOURCE_USERNAME:-postgres}
      SPRING_DATASOURCE_PASSWORD: ${SPRING_DATASOURCE_PASSWORD:-root}
      # 기본은 꺼짐. application.yml(기본 false)·DEPLOY_OCI.md와 맞춘다. compose만 true였던 탓에
      # 로컬 compose가 공유 Supabase의 flyway_schema_history를 바꿀 수 있었다(2026-07-26 장애).
      # 운영 서버는 .env에 SPRING_FLYWAY_ENABLED=true를 명시해 켠다.
      SPRING_FLYWAY_ENABLED: ${SPRING_FLYWAY_ENABLED:-false}
      SPRING_FLYWAY_BASELINE_VERSION: ${SPRING_FLYWAY_BASELINE_VERSION:-20260721.1}
      WORKFLOW_AI_FASTAPI_URL: http://backend-fastapi:8000
      # 어시스턴트 한 번 호출에 생성 LLM이 통째로 들어가 회의록 분석보다 오래 걸린다.
      # 여기서 끊기면 FastAPI가 만들어 둔 답이 버려지고 "일시적으로 처리할 수 없습니다"가 나간다.
      WORKFLOW_AI_ASSISTANT_READ_TIMEOUT_SECONDS: ${WORKFLOW_AI_ASSISTANT_READ_TIMEOUT_SECONDS:-120}
      RAG_INTERNAL_API_KEY: ${RAG_INTERNAL_API_KEY}
      JWT_SECRET: ${JWT_SECRET}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET}
      GOOGLE_REDIRECT_URI: ${GOOGLE_REDIRECT_URI:-http://localhost:8080/api/v1/auth/google/callback}
      WORKFLOW_FRONTEND_BASE_URL: ${WORKFLOW_FRONTEND_BASE_URL:-http://localhost:5173}
      WORKFLOW_DEMO_DEV_LOGIN_ENABLED: ${WORKFLOW_DEMO_DEV_LOGIN_ENABLED:-false}
      WORKFLOW_DEMO_SEED_ENABLED: ${WORKFLOW_DEMO_SEED_ENABLED:-true}
      SPRING_DATASOURCE_HIKARI_MAX_POOL_SIZE: ${SPRING_DATASOURCE_HIKARI_MAX_POOL_SIZE:-4}
      # S3 호환 오브젝트 스토리지(현재 Supabase Storage, 추후 OCI Object Storage).
      STORAGE_ENDPOINT: ${STORAGE_ENDPOINT:-}
      STORAGE_REGION: ${STORAGE_REGION:-us-east-1}
      STORAGE_ACCESS_KEY: ${STORAGE_ACCESS_KEY:-}
      STORAGE_SECRET_KEY: ${STORAGE_SECRET_KEY:-}
      STORAGE_BUCKET: ${STORAGE_BUCKET:-task-results}
      STORAGE_PATH_STYLE_ACCESS: ${STORAGE_PATH_STYLE_ACCESS:-true}
      # 비밀번호 재설정 메일 발송(SMTP). compose가 읽는 .env는 이 파일 안의 ${VAR} 치환에만 쓰이고
      # 컨테이너 환경으로 자동 주입되지 않는다 — 여기 선언하지 않으면 .env에 값을 채워도 JVM이 못 본다.
      # 이게 빠져 있어서 운영에서 workflow.mail.enabled가 기본값 false로 떨어졌고, MailConfig가
      # LoggingMailSender를 골라 재설정 메일이 로그에만 남고 실제로는 한 통도 나가지 않았다.
      # 기본은 꺼짐(application.yml과 동일) — 자격증명 없이 뜬 환경이 메일을 흘리면 안 되므로
      # 켜는 쪽이 명시적이어야 한다. 운영 서버는 .env에 WORKFLOW_MAIL_ENABLED=true를 명시해 켠다.
      WORKFLOW_MAIL_ENABLED: ${WORKFLOW_MAIL_ENABLED:-false}
      WORKFLOW_MAIL_HOST: ${WORKFLOW_MAIL_HOST:-}
      WORKFLOW_MAIL_PORT: ${WORKFLOW_MAIL_PORT:-587}
      WORKFLOW_MAIL_USERNAME: ${WORKFLOW_MAIL_USERNAME:-}
      WORKFLOW_MAIL_PASSWORD: ${WORKFLOW_MAIL_PASSWORD:-}
      # from이 비어 있으면 enabled=true여도 MailConfig가 LoggingMailSender로 떨어진다.
      WORKFLOW_MAIL_FROM: ${WORKFLOW_MAIL_FROM:-}
    ports:
      - '8080:8080'
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
      kafka:
        condition: service_started

  backend-fastapi:
    build:
      # Dockerfile이 App/backend_fastapi/app 소스도 함께 COPY하므로 빌드 컨텍스트를 루트로 잡는다
      context: ..
      dockerfile: App/backend_fastapi/Dockerfile
    container_name: workflow-backend-fastapi
    environment:
      # 기본은 backend-spring과 동일하게 compose 안의 로컬 db 서비스에 연결한다.
      # 컨테이너 안에서는 "localhost"가 컨테이너 자기 자신을 가리키므로, 로컬 개발용 .env에
      # DATABASE_URL=postgresql://...@localhost:5432/workflow처럼 host 실행 기준 값을 넣어뒀다면
      # ml_workload_score 등 FastAPI DB 조회가 전부 연결 실패로 죽는다(2026-07-24 실측).
      # Supabase(원격)에 직접 연결하려면 .env의 DATABASE_URL 값으로 이 기본값을 덮어쓴다.
      DATABASE_URL: ${DATABASE_URL:-postgresql://postgres:root@db:5432/workflow}
      # Supabase Session Pooler의 작은 세션 한도를 피한다. core.database_url이 Supabase
      # pooler 호스트의 5432만 6543(Transaction Pooler)로 바꾸며 로컬 DB에는 영향이 없다.
      DATABASE_USE_TRANSACTION_POOLER: ${DATABASE_USE_TRANSACTION_POOLER:-true}
      REDIS_URL: redis://redis:6379/0
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      # Spring(RagController) 외의 직접 호출로부터 /ai/rag/* 를 보호하는 서비스 간 공유 시크릿.
      # backend-spring과 반드시 같은 값이어야 한다 (llm_rag_assistant/app/security.py 참고).
      RAG_INTERNAL_API_KEY: ${RAG_INTERNAL_API_KEY}
      OLLAMA_HOST: ${OLLAMA_HOST:-http://host.docker.internal:11434}
      EMBEDDING_MODEL: ${EMBEDDING_MODEL:-nomic-embed-text}
      GENERATION_MODEL: ${GENERATION_MODEL:-gemma2}
      # RAG 챗봇 임베딩 모델 (embedding_service.py, 컨테이너 내부 로컬 추론)
      HF_EMBEDDING_MODEL: ${HF_EMBEDDING_MODEL:-rhantj/bge-m3-workflow-query-robust}
      # 위 모델의 커밋 revision 고정 (재현성 - 원격 저장소 갱신에 배포가 조용히 끌려가지 않게)
      HF_EMBEDDING_MODEL_REVISION: ${HF_EMBEDDING_MODEL_REVISION:-dc328732ab2c3330d38305199e26b2d060586af3}
      # 회의록 AI 분석 전용 (RAG Assistant의 GENERATION_MODEL과 분리)
      MEETING_ANALYSIS_PROVIDER: ${MEETING_ANALYSIS_PROVIDER:-auto}
      MEETING_ANALYSIS_MODEL: ${MEETING_ANALYSIS_MODEL:-qwen2.5:1.5b}
      MEETING_ANALYSIS_TIMEOUT_SECONDS: ${MEETING_ANALYSIS_TIMEOUT_SECONDS:-20}
      MEETING_ANALYSIS_MAX_CHARS: ${MEETING_ANALYSIS_MAX_CHARS:-6000}
      MEETING_ANALYSIS_NUM_PREDICT: ${MEETING_ANALYSIS_NUM_PREDICT:-650}
      MEETING_ANALYSIS_KEEP_ALIVE: ${MEETING_ANALYSIS_KEEP_ALIVE:-5m}
      OLLAMA_ANALYSIS_TEMPERATURE: ${OLLAMA_ANALYSIS_TEMPERATURE:-0.1}
      # FS-3 지연 위험도 모델(.pkl)을 Hugging Face Hub에서 내려받기 위한 설정 (docker-entrypoint.sh 참고)
      DELAY_RISK_HF_MODEL_REPO_ID: ${DELAY_RISK_HF_MODEL_REPO_ID:-}
      DELAY_RISK_HF_MODEL_REVISION: ${DELAY_RISK_HF_MODEL_REVISION:-}
      DELAY_RISK_HF_MODEL_SHA256: ${DELAY_RISK_HF_MODEL_SHA256:-}
      HF_TOKEN: ${HF_TOKEN:-}
      HUGGINGFACEHUB_API_TOKEN: ${HUGGINGFACEHUB_API_TOKEN:-${HF_TOKEN:-}}
      HF_MEETING_ANALYSIS_MODEL: ${HF_MEETING_ANALYSIS_MODEL:-Qwen/Qwen3-4B-Instruct-2507}
      # RAG 답변 생성 백엔드. 미지정이면 MEETING_ANALYSIS_PROVIDER를 따르되 RAG가 모르는 값
      # ("auto" 등)이면 huggingface로 남는다 (generation_service._resolve_provider).
      # HF 크레딧이 끊겼을 때 ollama로 두면 호스트의 로컬 모델로 답변이 계속 나간다.
      RAG_PROVIDER: ${RAG_PROVIDER:-}
      # 위가 ollama일 때 쓸 로컬 모델. GENERATION_MODEL(ai_contribution_report용)과 분리돼 있다.
      RAG_OLLAMA_MODEL: ${RAG_OLLAMA_MODEL:-gemma4:e2b}
      # 모델을 이만큼 메모리에 붙들어 둔다. ollama 기본값 5분으로는 질문 간격이 조금만 벌어져도
      # 모델이 내려가 다음 질문에 로드 시간이 그대로 얹힌다(웜 33초 → 콜드 44.6초, 2026-07-28 실측).
      RAG_OLLAMA_KEEP_ALIVE: ${RAG_OLLAMA_KEEP_ALIVE:-30m}
      HF_MEETING_ANALYSIS_ENDPOINT: ${HF_MEETING_ANALYSIS_ENDPOINT:-https://router.huggingface.co/v1/chat/completions}
      HF_MEETING_ANALYSIS_TIMEOUT_SECONDS: ${HF_MEETING_ANALYSIS_TIMEOUT_SECONDS:-35}
      # 900 토큰으로는 요약+결정사항+위험요소+투두 여러 건을 포함한 JSON이 완성되기 전에
      # 잘려서 파싱 실패(JSONDecodeError: Unterminated string)가 나던 문제 수정.
      HF_MEETING_ANALYSIS_MAX_TOKENS: ${HF_MEETING_ANALYSIS_MAX_TOKENS:-3000}
      HF_MEETING_ANALYSIS_TEMPERATURE: ${HF_MEETING_ANALYSIS_TEMPERATURE:-0.1}
      # LangSmith 트레이싱 (RAG 챗봇 langchain-huggingface ChatHuggingFace 실행 관찰용)
      LANGSMITH_TRACING: ${LANGSMITH_TRACING:-false}
      LANGSMITH_ENDPOINT: ${LANGSMITH_ENDPOINT:-}
      LANGSMITH_API_KEY: ${LANGSMITH_API_KEY:-}
      LANGSMITH_PROJECT: ${LANGSMITH_PROJECT:-}
    ports:
      - '8000:8000'
    volumes:
      # HF 임베딩 모델(bge-m3, 약 2.2GB)을 named volume에 유지한다. 없으면 재빌드/재생성마다
      # 기동 시 preload가 2.2GB를 새로 받느라 startup이 수십 분~수시간 막힌다(2026-07-24 실측).
      - hf-cache:/root/.cache/huggingface
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
      kafka:
        condition: service_started

  frontend:
    build:
      context: ./frontend
      args:
        VITE_ENABLE_DEMO_AUTH: ${VITE_ENABLE_DEMO_AUTH:-false}
    container_name: workflow-frontend
    # Dockerfile이 pnpm build 결과를 nginx(1.27-alpine)에 얹어 5173으로 서빙하고
    # /api/는 backend-spring:8080으로 프록시한다 (frontend/nginx.conf).
    # 정적 빌드라 HMR은 동작하지 않는다 — 프론트를 고치며 개발할 땐 이 컨테이너 대신
    # 호스트에서 `cd frontend && pnpm dev`를 쓸 것. 이 컨테이너로 확인하려면 재빌드해야 한다
    # (docker compose up -d --build frontend). 예전엔 ./frontend:/app 마운트와
    # API_PROXY_TARGET이 남아 있어 "반영됐겠지" 하고 옛 화면을 검증하게 만들었다(2026-07-28 실제 발생).
    ports:
      - '5173:5173'
    depends_on:
      - backend-spring

volumes:
  db-data:
  redis-data:
  hf-cache:
