-- ============================================================================
-- WorkFlow AI - [폐기됨] 마이페이지 "분야"를 다중 태그로 저장하도록 확장
-- 04_user_profile_fields.sql 이후에 실행됨.
--
-- ⚠ 이 마이그레이션은 폐기됐다. 원래는 users.field를 제자리에서 VARCHAR→JSONB로 바꿨는데,
-- 이는 공유 DB에서 구버전 백엔드(field를 String으로 매핑)를 기동 불가하게 만드는 파괴적
-- 변경이었다. 08_users_field_tags_column.sql이 그 자리를 대신해 새 컬럼 field_tags를
-- "추가"만 하고 field는 건드리지 않는 방식으로 바꿨다.
-- 이 파일은 번호 순서 보존을 위해서만 남겨두며, 의도적으로 아무 것도 실행하지 않는다 —
-- 초기화 스크립트를 처음부터(01→08) 실행하는 신규 환경에서도 field 컬럼 타입이 절대
-- 바뀌지 않도록 하기 위함이다.
-- ============================================================================
